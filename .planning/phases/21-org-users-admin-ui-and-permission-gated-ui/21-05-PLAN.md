---
phase: 21-org-users-admin-ui-and-permission-gated-ui
plan: 05
type: execute
wave: 3
depends_on:
  - 21-03
  - 21-04
files_modified: []
autonomous: false
requirements:
  - USER-01
  - USER-05
  - USER-06
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "All 7 success criteria for Phase 21 are verifiable by a human using the running app"
    - "No TypeScript errors and yarn build passes"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all Phase 21 functionality: org users admin page (member list, role change, user removal) and permission-gated UI (Viewer restrictions on create, GitHub, Figma, and token editing).

Purpose: Final gate before marking Phase 21 complete. Tests the full round-trip of role propagation and all UI permission gates.
Output: Human approval confirming all 7 success criteria.
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final build verification</name>
  <files></files>
  <action>
Run a clean TypeScript check and production build to confirm no errors before human verification:

```bash
cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit && yarn build
```

If either command fails, fix the reported errors before presenting the checkpoint. Common issues to check:
- Missing `roleLastFetched` in next-auth.d.ts JWT interface
- `date-fns` import in org/users/page.tsx (replace with toLocaleDateString if present)
- Alert dialog or Select component import paths
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit && echo "TS OK" && yarn build 2>&1 | tail -5</automated>
  </verify>
  <done>npx tsc --noEmit exits with code 0. yarn build completes without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all Phase 21 success criteria</name>
  <files></files>
  <action>
Present the verification checklist to the human. Start the dev server first: `cd /Users/user/Dev/atui-tokens-manager && yarn dev`

CHECKPOINT: Verification Required — test all 7 success criteria listed in how-to-verify below.
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && yarn dev &amp; sleep 5 &amp;&amp; echo "Dev server started"</automated>
    <manual>
      **Test 1 — Org Users page (USER-01):**
      1. Sign in as an Admin user and navigate to /org/users
      2. Confirm a "Members" section appears above "Pending Invitations"
      3. Confirm each member row shows: name, email, role selector, status badge (Active/Invited), Remove button

      **Test 2 — Role change (USER-05):**
      1. On the Members table, change a non-superadmin user's role using the inline selector
      2. Confirm the row updates optimistically with no error toast
      3. Sign in as that user — within 60 seconds their permissions reflect the new role

      **Test 3 — Remove user (USER-06):**
      1. Click the Remove (trash) button on a non-superadmin, non-self user
      2. Confirm the AlertDialog appears; clicking "Remove" removes the row
      3. On the removed user's next page request, they are redirected to sign-in

      **Test 4 — Superadmin and self protection:**
      1. Confirm the superadmin row has role selector and Remove button disabled
      2. Confirm the Admin's own row has Remove button disabled

      **Test 5 — Viewer: Collections page (UI-02):**
      1. Sign in as a Viewer; navigate to /collections
      2. Confirm "New Collection" button and dashed card are NOT visible
      3. Confirm existing collection cards are still visible

      **Test 6 — Viewer: GitHub and Figma controls (UI-03, UI-04):**
      1. As a Viewer, open any collection tokens page; click the EllipsisVertical dropdown
      2. Confirm Import from GitHub, Push to GitHub, Import from Figma, Export to Figma are NOT in the menu

      **Test 7 — Viewer: Token editing disabled (UI-01):**
      1. As a Viewer, on a collection tokens page
      2. Confirm token value input fields are visible but non-interactive (disabled/read-only)
      3. Confirm no bulk action bar appears
    </manual>
  </verify>
  <done>Human types "approved" confirming all 7 tests pass.</done>
</task>

</tasks>

<verification>
All 7 phase success criteria verified by human:
1. Admin sees all org members with name, email, role, status
2. Admin can change role; change propagates within 60s without sign-out
3. Admin can remove user; removed user loses access on next request
4. Token edit controls are disabled (not hidden) for read-only users
5. New Collection button/card hidden for Viewers
6. GitHub controls hidden for Viewers
7. Figma controls hidden for Viewers
</verification>

<success_criteria>
Human approves all 7 verification steps. Phase 21 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/21-org-users-admin-ui-and-permission-gated-ui/21-05-SUMMARY.md`
</output>
