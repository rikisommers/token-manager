---
phase: 04-collection-management
plan: "04"
type: execute
wave: 2
depends_on:
  - "04-02"
files_modified:
  - src/app/collections/[id]/layout.tsx
  - src/app/collections/[id]/tokens/page.tsx
  - src/app/collections/[id]/config/page.tsx
  - src/components/CollectionSidebar.tsx
autonomous: true
requirements:
  - COL-03
  - COL-08

must_haves:
  truths:
    - "Navigating to /collections/[id]/tokens shows the Tokens page scoped to that collection, with the collection name in the heading"
    - "A persistent sidebar shows the collection name, a 'Collections' back link, and 3 nav items (Tokens, Config, Settings)"
    - "Clicking 'Collections' in the sidebar navigates back to /collections"
    - "Active nav item is highlighted in the sidebar"
    - "The collection-scoped layout does NOT show the root AppSidebar or AppHeader"
    - "/collections/[id]/config shows the Configuration page (BuildTokensPanel) scoped to the collection"
  artifacts:
    - path: "src/app/collections/[id]/layout.tsx"
      provides: "Layout shell with CollectionSidebar for all collection-scoped pages"
      contains: "CollectionSidebar"
    - path: "src/components/CollectionSidebar.tsx"
      provides: "Sidebar with collection name, back link, 3 nav items"
      contains: "Collections"
    - path: "src/app/collections/[id]/tokens/page.tsx"
      provides: "Tokens page scoped to collection — shows collection name in header"
      contains: "tokens"
    - path: "src/app/collections/[id]/config/page.tsx"
      provides: "Configuration page scoped to collection — renders BuildTokensPanel"
      contains: "BuildTokensPanel"
  key_links:
    - from: "src/app/collections/[id]/layout.tsx"
      to: "/api/collections/[id]"
      via: "fetch on mount to get collection name for sidebar header"
      pattern: "api/collections"
    - from: "src/components/CollectionSidebar.tsx"
      to: "/collections"
      via: "Link back to grid"
      pattern: "href.*collections"
    - from: "src/app/collections/[id]/tokens/page.tsx"
      to: "src/app/page.tsx (former home)"
      via: "port the existing tokens page logic, scope it to the collection id from params"
      pattern: "params\\.id"
---

<objective>
Create the collection-scoped routing shell: /collections/[id]/layout.tsx with a dedicated CollectionSidebar, and stub out the Tokens and Config pages. The sidebar replaces the root AppSidebar for collection-scoped routes.

Purpose: Each collection needs its own navigation context — collection name in header, back to grid, and 3 nav items.
Output: Collection layout, CollectionSidebar, Tokens page (scoped), Config page (scoped).
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/AppSidebar.tsx
@src/app/configuration/page.tsx
@src/app/page.tsx
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: CollectionSidebar component and collection-scoped layout</name>
  <files>
    src/components/CollectionSidebar.tsx
    src/app/collections/[id]/layout.tsx
  </files>
  <action>
Create `src/components/CollectionSidebar.tsx` as a 'use client' component.

Props: `{ collectionId: string; collectionName: string }`

Structure (mirrors AppSidebar styling — bg-white, border-r, same width):
```
aside.h-full.bg-white.border-r.border-gray-200.flex.flex-col
  [top section] px-4 py-4
    Link href="/collections" className="flex items-center gap-1 text-xs text-gray-500 hover:text-gray-700 mb-3"
      ChevronLeft icon (size=14) + "Collections"
    [collection name] text-gray-900 font-semibold text-sm truncate
  [nav] flex-1 px-2 py-2 space-y-1
    3 nav links using usePathname() for active detection:
      - /collections/[id]/tokens → Palette icon, "Tokens"
      - /collections/[id]/config → Settings2 icon, "Config"
      - /collections/[id]/settings → SlidersHorizontal icon, "Settings"
    Active link: bg-gray-900 text-white (same pattern as AppSidebar)
    Inactive link: text-gray-600 hover:bg-gray-100 hover:text-gray-900
```

Use lucide-react icons: ChevronLeft, Palette, Settings2, SlidersHorizontal.

Create `src/app/collections/[id]/layout.tsx` as a 'use client' component (needs useEffect for fetching name, OR accept params and pass to a child).

Actually, use this pattern: layout.tsx is a server component wrapper that passes params.id down. BUT to fetch the collection name and show it in the sidebar, use a client component approach:

`src/app/collections/[id]/layout.tsx`:
- Server component
- Accepts `{ children, params }: { children: React.ReactNode; params: { id: string } }`
- Renders `<CollectionLayoutClient id={params.id}>{children}</CollectionLayoutClient>`

`src/components/CollectionLayoutClient.tsx` (create this too):
- 'use client' component
- Accepts `{ id: string; children: React.ReactNode }`
- useEffect: fetch GET /api/collections/[id] to get the collection name
- Stores `collectionName: string` in state (default: "Loading..." or "")
- Renders the full layout shell:
  ```
  div.flex.h-screen.overflow-hidden.bg-gray-50
    div.w-[200px].flex-shrink-0
      CollectionSidebar collectionId={id} collectionName={collectionName}
    main.flex-1.overflow-y-auto
      {children}
  ```
- The root layout LayoutShell must NOT add its own sidebar/header for /collections/[id]/* routes — update LayoutShell in the same task to also suppress sidebar for any pathname starting with /collections

IMPORTANT: Update `src/components/LayoutShell.tsx` (created in plan 04-02): change the condition from `pathname === '/collections'` to `pathname.startsWith('/collections')`. This ensures the root sidebar/header is suppressed for ALL /collections/* routes (the collection-scoped layout provides its own).

Files to also update: `src/components/LayoutShell.tsx` — add this to the files list.
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit 2>&1 | grep -v "token-manager-angular\|token-manager-stencil\|token.service.ts" | head -20</automated>
    <manual>With dev server: visit /collections/[any-valid-id]/tokens — should see the CollectionSidebar with collection name, "Collections" link, and 3 nav items. Root AppSidebar should NOT be visible. Clicking "Collections" goes to /collections.</manual>
  </verify>
  <done>CollectionSidebar renders with collection name, back link, and 3 nav items with active highlighting. Collection-scoped layout shows CollectionSidebar + main content area. Root sidebar hidden for all /collections/* routes. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Collection-scoped Tokens page and Config page</name>
  <files>
    src/app/collections/[id]/tokens/page.tsx
    src/app/collections/[id]/config/page.tsx
  </files>
  <action>
Create `src/app/collections/[id]/tokens/page.tsx` as a 'use client' component.

This is the Tokens page scoped to a specific collection. Port the logic from `src/app/page.tsx` (the former home page tokens view) but scope it to the collection ID from params.

Accept params: `{ params: { id: string } }`

Key adaptations from the old page.tsx:
- Page heading: `<h1>Tokens: {collectionName}</h1>` — fetch collection name on mount via GET /api/collections/[id]
- Load tokens: use the existing token loading pattern — but show the collection's tokens field (from the collection object) rather than the flat filesystem tokens. If the collection has a `tokens` object, render it using the existing TokenTable component.
- Collection name shown in the main content page header as: `"Tokens: [Collection Name]"` (per CONTEXT.md decision)
- Keep all existing functionality: CollectionActions (rename, delete, duplicate, save-as etc.), token table, generate form if already present on the page
- For now: if the existing page.tsx had complex logic, port the minimal viable version — show tokens loaded from GET /api/collections/[id] (the tokens field), rendered in a TokenTable, with the collection name header
- The generate/export/Figma actions are not part of this plan — they remain on the old /generate route until Phase 4 is complete; do not add them here now

Page layout:
```
div className="p-6"
  h1 className="text-xl font-bold text-gray-900 mb-4": "Tokens: {collectionName}"
  [tokens content — TokenTable or empty state]
```

Empty state when tokens is empty object: show "No tokens yet. Use the Config page to build tokens or import from GitHub/Figma."

Create `src/app/collections/[id]/config/page.tsx` as a 'use client' component.

Port logic from `src/app/configuration/page.tsx` but scope it to the collection ID from params.

Accept params: `{ params: { id: string } }`

Key change: Instead of reading `atui-selected-collection-id` from localStorage, use `params.id` directly.
Fetch collection tokens via GET /api/collections/[id], pass the tokens to BuildTokensPanel.

Layout mirrors the existing configuration page (two-column: left = Build Settings info, right = BuildTokensPanel):
- Left column heading: "Build Settings" with description text
- Right column: `<BuildTokensPanel tokens={tokens} />`
- Collection name in the page heading: `"Configuration: {collectionName}"`
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit 2>&1 | grep -v "token-manager-angular\|token-manager-stencil\|token.service.ts" | head -20</automated>
    <manual>With dev server: visit /collections/[valid-id]/tokens — shows "Tokens: [name]" heading and the collection's token data. Visit /collections/[valid-id]/config — shows two-column layout with BuildTokensPanel on right.</manual>
  </verify>
  <done>/collections/[id]/tokens renders with collection name in heading and tokens displayed. /collections/[id]/config renders two-column layout with BuildTokensPanel. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` passes with no new errors.
/collections/[id]/tokens shows CollectionSidebar + tokens content headed "Tokens: [name]".
/collections/[id]/config shows CollectionSidebar + two-column config layout.
Root sidebar is suppressed for all /collections/* routes.
"Collections" back link in sidebar navigates to /collections.
</verification>

<success_criteria>
- CollectionSidebar shows collection name, Collections back link, and 3 active-highlighted nav items
- /collections/[id]/tokens scoped to collection with name in heading
- /collections/[id]/config renders BuildTokensPanel for the collection
- Root AppSidebar/AppHeader hidden on all /collections/* routes
- Zero new TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-collection-management-grid-of-collections-collection-scoped-pages-with-sidebar-tokens-config-settings-per-collection-config-persisted-to-db/04-04-SUMMARY.md`
</output>
