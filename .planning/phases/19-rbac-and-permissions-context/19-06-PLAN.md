---
phase: 19-rbac-and-permissions-context
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/github/branches/route.ts
autonomous: true
requirements:
  - PERM-03
gap_closure: true

must_haves:
  truths:
    - "GET /api/github/branches returns 401/403 for unauthenticated or Viewer sessions"
    - "GET /api/github/branches continues to return branch data for Editor and Admin sessions"
    - "POST /api/github/branches guard is unchanged"
  artifacts:
    - path: "src/app/api/github/branches/route.ts"
      provides: "GET handler with requireRole(Action.PushGithub) guard at top of handler body"
      contains: "requireRole(Action.PushGithub)"
  key_links:
    - from: "src/app/api/github/branches/route.ts GET"
      to: "src/lib/auth/require-auth.ts"
      via: "requireRole(Action.PushGithub)"
      pattern: "requireRole\\(Action\\.PushGithub\\)"

# Gap #2 disposition:
# CONTEXT.md explicitly locked: "Changes take effect on next sign-in (no polling, no real-time push)."
# The ROADMAP success criterion #5 ("within 60 seconds") conflicts with this locked decision.
# Per context_fidelity rules, the locked user decision supersedes ROADMAP wording.
# PermissionsContext.tsx fetches /permissions/me on route change — no polling required.
# NO action is taken for gap #2.
---

<objective>
Guard the GET handler in src/app/api/github/branches/route.ts with requireRole(Action.PushGithub).

Purpose: The VERIFICATION.md identified that Plan 03 gated only the POST handler; the GET handler at line 79 has no auth check. This violates PERM-03 (Viewer has no GitHub operations) — any session or no session can currently list GitHub branches.

Output: Single-line fix mirroring the POST guard pattern already present at line 7. POST remains unchanged.
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-rbac-and-permissions-context/19-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add requireRole guard to GET /api/github/branches</name>
  <files>src/app/api/github/branches/route.ts</files>
  <action>
Insert the requireRole guard at the very top of the GET handler body, immediately after the opening `try {` brace opens — matching the pattern already used by the POST handler at line 7.

Current GET handler (line 79+):
```ts
export async function GET(request: NextRequest) {
  try {
    const { searchParams } = new URL(request.url);
```

After fix:
```ts
export async function GET(request: NextRequest) {
  const authResult = await requireRole(Action.PushGithub);
  if (authResult instanceof NextResponse) return authResult;
  try {
    const { searchParams } = new URL(request.url);
```

The two imports at the top of the file (`requireRole` from `@/lib/auth/require-auth` and `Action` from `@/lib/auth/permissions`) are already present — no new imports needed.

Do NOT modify the POST handler, the import block, or any other part of the file.
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && grep -n "requireRole" src/app/api/github/branches/route.ts</automated>
    <manual>Output must show TWO lines containing requireRole: one for POST (line ~7) and one for GET (line ~80). Then run: yarn build 2>&1 | tail -5 — build must exit 0 with no TypeScript errors.</manual>
    <sampling_rate>run immediately after edit, before summary</sampling_rate>
  </verify>
  <done>
    src/app/api/github/branches/route.ts contains requireRole(Action.PushGithub) in both the POST handler and the GET handler. yarn build passes with zero TypeScript errors. Unauthenticated and Viewer callers to GET /api/github/branches receive 401/403; Editor and Admin callers receive branch data.
  </done>
</task>

</tasks>

<verification>
After Task 1:

1. `grep -n "requireRole" src/app/api/github/branches/route.ts` — must return exactly 2 matches (POST and GET handlers).
2. `yarn build` — must exit 0 with zero TypeScript errors.
3. No other files modified (git diff --name-only must show only branches/route.ts).

Gap #2 disposition (no code action): CONTEXT.md locked decision — "changes take effect on next sign-in (no polling)" — supersedes ROADMAP 60-second propagation wording. PermissionsContext.tsx is correct as-is.
</verification>

<success_criteria>
- GET /api/github/branches is gated with requireRole(Action.PushGithub) — matching POST handler pattern
- Unauthenticated session: receives 401 Unauthorized
- Viewer session: receives 403 Forbidden
- Editor/Admin session: receives branch data normally
- yarn build passes with zero TypeScript errors
- PERM-03 is fully satisfied (Viewer has no GitHub operations on any handler)
</success_criteria>

<output>
After completion, create `.planning/phases/19-rbac-and-permissions-context/19-06-SUMMARY.md`
</output>
