---
phase: 19-rbac-and-permissions-context
plan: "03"
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - src/app/api/export/github/route.ts
  - src/app/api/export/figma/route.ts
  - src/app/api/github/branches/route.ts
  - src/app/api/figma/import/route.ts
  - src/app/api/import/github/route.ts
  - src/app/api/build-tokens/route.ts
  - src/app/api/tokens/[...path]/route.ts
  - src/app/api/database/test/route.ts
  - src/app/api/database/config/route.ts
autonomous: true
requirements:
  - PERM-01
  - PERM-02
  - PERM-03

must_haves:
  truths:
    - "POST /api/export/github returns 403 for Viewer (no PushGithub permission)"
    - "POST /api/export/figma returns 403 for Viewer (no PushFigma permission)"
    - "POST /api/export/github succeeds for Editor (Editor has PushGithub)"
    - "All 9 route files compile without TypeScript errors after upgrade"
  artifacts:
    - path: "src/app/api/export/github/route.ts"
      provides: "requireRole(Action.PushGithub) — org-level check"
      contains: "requireRole"
    - path: "src/app/api/export/figma/route.ts"
      provides: "requireRole(Action.PushFigma) — org-level check"
      contains: "requireRole"
    - path: "src/app/api/build-tokens/route.ts"
      provides: "requireRole(Action.Write) — org-level check"
      contains: "requireRole"
  key_links:
    - from: "src/app/api/export/github/route.ts"
      to: "src/lib/auth/require-auth.ts"
      via: "requireRole(Action.PushGithub) — no collectionId (org-level)"
      pattern: "requireRole.*PushGithub"
    - from: "src/app/api/export/figma/route.ts"
      to: "src/lib/auth/require-auth.ts"
      via: "requireRole(Action.PushFigma) — no collectionId (org-level)"
      pattern: "requireRole.*PushFigma"
---

<objective>
Upgrade all non-collection-scoped write route handlers to use requireRole() with appropriate org-level action checks, replacing the existing requireAuth() calls.

Purpose: These routes (export, import, build, tokens proxy, database utilities) operate at org level and don't need a per-collection grant check. They need role enforcement: Viewer cannot push to GitHub/Figma, Editor can.
Output: 9 upgraded route files with requireRole() replacing requireAuth().
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-rbac-and-permissions-context/19-CONTEXT.md
@.planning/phases/19-rbac-and-permissions-context/19-RESEARCH.md
@.planning/phases/19-rbac-and-permissions-context/19-01-SUMMARY.md
@src/lib/auth/permissions.ts
@src/lib/auth/require-auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade export and import route handlers</name>
  <files>
    src/app/api/export/github/route.ts
    src/app/api/export/figma/route.ts
    src/app/api/github/branches/route.ts
    src/app/api/figma/import/route.ts
    src/app/api/import/github/route.ts
  </files>
  <action>
Read each file and replace `requireAuth()` with the appropriate `requireRole(action)` call. These are all org-level checks — no `collectionId` argument needed. The collection access was already gated at the UI layer before the user can trigger these actions.

Action mappings (no collectionId for all):
- `POST /api/export/github` → `requireRole(Action.PushGithub)` — GitHub push requires PushGithub; Viewer cannot push
- `POST /api/export/figma` → `requireRole(Action.PushFigma)` — Figma push requires PushFigma; Viewer cannot push
- `GET /api/github/branches` → `requireRole(Action.PushGithub)` — fetching branch list is a GitHub operation; same gate as push
- `POST /api/figma/import` → `requireRole(Action.PushFigma)` — Figma import/export; same gate as push
- `POST /api/import/github` → `requireRole(Action.PushGithub)` — importing from GitHub; same gate as push

For each file:
1. Read the current file content.
2. Replace `import { requireAuth } from '@/lib/auth/require-auth'` with `import { requireRole } from '@/lib/auth/require-auth'`.
3. Add `import { Action } from '@/lib/auth/permissions'` (check if already imported).
4. Replace `const authResult = await requireAuth()` with `const authResult = await requireRole(Action.XxxXxx)` using the mapping above.
5. The `if (authResult instanceof NextResponse) return authResult` guard line stays identical.

Note: `GET /api/github/branches` — verify it currently uses requireAuth on a GET handler (unusual but present per Phase 18 plan). This is fine — it accesses external GitHub API and represents a privileged operation. Keep the upgrade to requireRole(Action.PushGithub).
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>grep -rn "requireAuth\|requireRole" src/app/api/export/ src/app/api/github/ src/app/api/figma/ src/app/api/import/ — all should show requireRole after upgrade</manual>
  </verify>
  <done>All 5 export/import route files use requireRole() with the correct Action; no requireAuth() remains in these files; TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade utility route handlers</name>
  <files>
    src/app/api/build-tokens/route.ts
    src/app/api/tokens/[...path]/route.ts
    src/app/api/database/test/route.ts
    src/app/api/database/config/route.ts
  </files>
  <action>
Read each file and replace `requireAuth()` with `requireRole(action)`. These are utility/internal routes.

Action mappings:
- `POST /api/build-tokens` → `requireRole(Action.Write)` — building tokens is a write operation; Viewer cannot trigger builds
- `src/app/api/tokens/[...path]/route.ts` — read the file to see what HTTP methods are guarded. Likely GET or POST for token proxy/lookup. Apply `requireRole(Action.Read)` if it's a read proxy, or `requireRole(Action.Write)` if it modifies. Check the file first — use the most restrictive appropriate action. If it's a proxy serving token data for display (Read), use `requireRole(Action.Read)`. Note: All roles including Viewer can Read, so `requireRole(Action.Read)` just ensures there's a session without role restriction beyond that.
- `POST /api/database/test` → `requireRole(Action.ManageUsers)` — database test/config routes are admin-only operations; ManageUsers is Admin-only per permissions.ts
- `PUT/POST /api/database/config` → `requireRole(Action.ManageUsers)` — same as above; admin-only

For each file:
1. Read the current file content.
2. Identify every method using `requireAuth()`.
3. Replace with the appropriate `requireRole(Action.Xxx)` per the mappings above.
4. Update imports: replace `requireAuth` with `requireRole` in the import; add `Action` import from `@/lib/auth/permissions`.

After reading `src/app/api/tokens/[...path]/route.ts`, use judgment on the correct action based on what the handler does. If it's a passthrough/proxy for reading token values, use `Action.Read`. If it creates or modifies tokens, use `Action.Write`.

Read `src/app/api/database/test/route.ts` and `src/app/api/database/config/route.ts` to confirm the HTTP methods and current requireAuth usage before upgrading.
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>grep -rn "requireAuth\|requireRole" src/app/api/build-tokens/ src/app/api/tokens/ src/app/api/database/ — all should show requireRole after upgrade</manual>
  </verify>
  <done>All 4 utility route files use requireRole() with appropriate Actions; no requireAuth() remains in these files; TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
Run: `cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit 2>&1`
Expected: Zero TypeScript errors.

Verify no stale requireAuth outside the auth/ module itself and /api/auth/setup bootstrap exception:
`grep -rn "requireAuth" src/app/api/ --include="*.ts"` — should return only `/api/auth/setup/route.ts` (bootstrap exception, documented in Phase 18).

Verify requireRole used in all upgraded files:
`grep -rn "requireRole" src/app/api/ --include="*.ts"` — should show all 9 upgraded files.
</verification>

<success_criteria>
1. All 9 files in this plan use requireRole() instead of requireAuth()
2. Export routes use Action.PushGithub / Action.PushFigma (Viewer gets 403)
3. Database utility routes use Action.ManageUsers (Editor/Viewer get 403)
4. Only /api/auth/setup remains using requireAuth() — all other write routes use requireRole()
5. npx tsc --noEmit passes
</success_criteria>

<output>
After completion, create `.planning/phases/19-rbac-and-permissions-context/19-03-SUMMARY.md`
</output>
