---
phase: 19-rbac-and-permissions-context
plan: "05"
type: execute
wave: 3
depends_on:
  - "19-02"
  - "19-03"
  - "19-04"
files_modified: []
autonomous: false
requirements:
  - PERM-01
  - PERM-02
  - PERM-03
  - PERM-04
  - PERM-05
  - PERM-06

must_haves:
  truths:
    - "Admin user can view and edit all collections without restriction"
    - "Viewer user cannot access write API endpoints (receives 403)"
    - "Non-Admin user cannot see collections they have no grant for"
    - "usePermissions() hook is accessible and returns correct booleans in running app"
  artifacts:
    - path: "src/lib/auth/require-auth.ts"
      provides: "requireRole() in production"
      exports: ["requireRole", "requireAuth", "AuthResult"]
    - path: "src/context/PermissionsContext.tsx"
      provides: "usePermissions() returning named booleans"
      exports: ["PermissionsProvider", "usePermissions"]
  key_links:
    - from: "Browser (Admin user)"
      to: "/api/collections"
      via: "sees all collections"
      pattern: "n/a"
    - from: "Browser (non-Admin without grant)"
      to: "/api/collections/[id]"
      via: "receives 404"
      pattern: "n/a"
---

<objective>
Human verification of Phase 19 RBAC implementation — confirm role enforcement is working end-to-end across API routes and the PermissionsContext.

Purpose: Visual and behavioral verification that the RBAC layer is correctly enforced: Admins have full access, Viewers are blocked on writes, non-granted collections are invisible.
Output: Phase 19 signed off by user.
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-rbac-and-permissions-context/19-CONTEXT.md
@.planning/phases/19-rbac-and-permissions-context/19-02-SUMMARY.md
@.planning/phases/19-rbac-and-permissions-context/19-03-SUMMARY.md
@.planning/phases/19-rbac-and-permissions-context/19-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and curl tests</name>
  <files></files>
  <action>
Run the following verification steps to confirm the implementation before the human checkpoint:

**1. TypeScript build:**
```bash
cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit
```
Must pass with zero errors.

**2. Confirm requireRole coverage:**
```bash
grep -rn "requireAuth\b" src/app/api/ --include="*.ts" | grep -v "/api/auth/setup"
```
Expected: No output (only /api/auth/setup uses requireAuth — the bootstrap exception).

**3. Confirm requireRole coverage:**
```bash
grep -rn "requireRole" src/app/api/ --include="*.ts" | wc -l
```
Expected: 17+ lines (all write handlers upgraded).

**4. Confirm PermissionsContext API:**
```bash
grep -n "canEdit\|canCreate\|isAdmin\|canGitHub\|canFigma" src/context/PermissionsContext.tsx | head -20
```
Expected: All 5 boolean fields present in PermissionsContextValue.

**5. Start dev server and run curl tests:**
```bash
cd /Users/user/Dev/atui-tokens-manager && yarn dev &
sleep 5
```

Unauthenticated collection list (should return 401):
```bash
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/collections
```
Expected: 401

Unauthenticated write (should return 401):
```bash
curl -s -X POST -H "Content-Type: application/json" -d '{"name":"test"}' -o /dev/null -w "%{http_code}" http://localhost:3000/api/collections
```
Expected: 401

Report curl results in the summary. Kill dev server after tests.
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit 2>&1</automated>
    <manual>Zero TypeScript errors; curl tests return 401 for unauthenticated requests</manual>
  </verify>
  <done>TypeScript build passes; unauthenticated API requests return 401; requireAuth replaced by requireRole in all non-bootstrap route handlers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of RBAC end-to-end</name>
  <files></files>
  <action>
Pause for human verification. Claude should present the what-built summary and how-to-verify steps to the user, then wait for their approval signal before proceeding.
  </action>
  <verify>
    <automated>MISSING — human approval required; no automated check possible for visual/behavioral verification</automated>
    <manual>
Run `yarn dev` and verify these scenarios:

Scenario 1: Admin access (sign in as your Admin account)
1. Navigate to the collections list — all collections should be visible
2. Open any collection — can see token table and graph without errors
3. Check browser console — no errors from usePermissions or PermissionsProvider
4. Open DevTools Network tab — navigate to a collection page and look for the /api/collections/[id]/permissions/me request; it should return { "role": "Admin" }

Scenario 2: API role enforcement (use curl or browser DevTools)
Without signing in (no session cookie):
- curl http://localhost:3000/api/collections should return HTTP 401
- curl -X POST http://localhost:3000/api/collections should return HTTP 401

Scenario 3: Bootstrap verification
1. Sign in as Admin and navigate to the collections list
2. Server logs should show no errors from bootstrapCollectionGrants()
3. If pre-existing collections exist, CollectionPermission records should now exist in MongoDB

Scenario 4: usePermissions() hook wired correctly
1. Sign in as Admin and navigate to a collection page
2. No console errors about usePermissions or context
3. App loads and functions normally (token table visible, no broken UI)
    </manual>
  </verify>
  <done>User types "approved" — all 4 verification scenarios pass without errors.</done>
</task>

</tasks>

<verification>
Phase 19 is complete when:
1. npx tsc --noEmit passes with zero errors
2. All write route handlers use requireRole() (only /api/auth/setup exempt)
3. Unauthenticated API requests return 401
4. User approves the human-verify checkpoint
</verification>

<success_criteria>
1. Admin can access all collections and perform all operations
2. GET /api/collections returns 401 for unauthenticated requests
3. requireAuth() usage is limited to /api/auth/setup only
4. usePermissions() is exported from PermissionsContext.tsx with named booleans
5. Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/19-rbac-and-permissions-context/19-05-SUMMARY.md`
</output>
