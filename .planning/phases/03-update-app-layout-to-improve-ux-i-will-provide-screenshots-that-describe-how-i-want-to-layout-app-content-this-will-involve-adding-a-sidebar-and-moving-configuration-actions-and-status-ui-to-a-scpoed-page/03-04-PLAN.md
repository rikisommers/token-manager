---
phase: 03-app-layout-ux
plan: "04"
type: execute
wave: 3
depends_on:
  - "03-02"
  - "03-03"
files_modified: []
autonomous: false
requirements:
  - LAYOUT-07

must_haves:
  truths:
    - "User sees the sidebar on every page with correct active nav highlighting"
    - "Tokens page shows Generate Tokens as primary content with collection action bar"
    - "Configuration page shows inline build output (no modal)"
    - "Settings page shows Figma and GitHub forms always expanded"
    - "Sidebar collection selector works — changing selection updates token display"
    - "Build Tokens produces downloadable output on Configuration page"
---

<objective>
Human visual verification of the complete Phase 3 layout. Confirm all three pages render correctly with the sidebar, that interactive flows work (collection switching, building tokens, saving config), and that the layout matches the CONTEXT decisions.

Purpose: This is a layout/UX change — automated tests can't verify visual correctness. A human must confirm the sidebar, page layouts, and interactions work as designed.
Output: Human approval or list of issues to fix
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-update-app-layout-to-improve-ux-i-will-provide-screenshots-that-describe-how-i-want-to-layout-app-content-this-will-involve-adding-a-sidebar-and-moving-configuration-actions-and-status-ui-to-a-scpoed-page/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final build check</name>
  <files></files>
  <action>
Run a final TypeScript check and development build to confirm no new errors were introduced by Phase 3 work:

```bash
cd /Users/user/Dev/atui-tokens-manager
npx tsc --noEmit 2>&1
```

Count total TypeScript errors. There should be at most 1 (the pre-existing `src/services/token.service.ts` line 131 string index error). If more errors exist, fix them before proceeding to the checkpoint.

Also run:
```bash
yarn dev &
sleep 5
curl -s http://localhost:3000 | grep -c "html" && echo "App is running"
```

Stop the dev server after confirming it starts.
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit 2>&1 | grep -c "error TS" | xargs -I{} sh -c 'echo "TypeScript errors: {}"; if [ {} -le 1 ]; then echo "PASS"; else echo "FAIL — fix before checkpoint"; fi'</automated>
  </verify>
  <done>TypeScript check passes with at most 1 pre-existing error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of Phase 3 layout</name>
  <what-built>
Complete Phase 3 layout:
- Persistent left sidebar with app name, collection selector, and 3 nav items (Tokens, Configuration, Settings)
- Tokens page (/) — Generate Tokens as primary, collection action top bar, no old header or tab switcher
- Configuration page (/configuration) — inline Build Tokens output (two-column)
- Settings page (/settings) — Figma + GitHub forms always expanded
  </what-built>
  <how-to-verify>
Run the app: `yarn dev` in the project directory, then visit each page.

1. **Sidebar** (visible on all pages)
   - App name "ATUI Tokens" at top
   - Collection selector dropdown below app name
   - Three nav items with icons: Tokens, Configuration, Settings
   - Active item has filled dark background (not just underline)
   - Sidebar stays fixed while main content scrolls

2. **Tokens page** (http://localhost:3000)
   - Top bar shows: Save As, New Collection, and collection action buttons (Delete/Rename/Duplicate)
   - SourceContextBar visible below top bar (shows upstream GitHub info when a collection with source metadata is selected)
   - Generate Tokens form is the main content (not behind a tab)
   - No "Design Token Manager" header visible
   - No "View Tokens / Generate Tokens" tab switcher visible
   - Collection selector in sidebar works — switching collections updates the page

3. **Configuration page** (http://localhost:3000/configuration)
   - Two-column layout: left shows collection name and build settings, right shows build output panel
   - Click "Build Tokens" button — output appears inline (not in a modal)
   - Format tabs (CSS, SCSS, LESS, JS, TS, JSON) work in the inline panel
   - "Download All" button downloads a ZIP file

4. **Settings page** (http://localhost:3000/settings)
   - Figma section: form fields always visible (no expand button)
   - GitHub section: form fields always visible (no expand button)
   - Both forms retain saved values from localStorage

Type "approved" if everything looks correct, or describe specific issues to fix.
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 3, or describe issues found during verification.</resume-signal>
</task>

</tasks>

<verification>
Human approval of all four verification areas above.
</verification>

<success_criteria>
- Human types "approved" confirming the layout matches the CONTEXT decisions
- All three pages render correctly with the sidebar
- Interactive flows (collection switch, build tokens, settings save) work
</success_criteria>

<output>
After approval, create `.planning/phases/03-update-app-layout-to-improve-ux-i-will-provide-screenshots-that-describe-how-i-want-to-layout-app-content-this-will-involve-adding-a-sidebar-and-moving-configuration-actions-and-status-ui-to-a-scpoed-page/03-04-SUMMARY.md`
</output>
