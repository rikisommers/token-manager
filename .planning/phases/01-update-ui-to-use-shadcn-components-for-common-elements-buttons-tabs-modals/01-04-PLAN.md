---
phase: 01-update-ui-to-use-shadcn-components-for-common-elements-buttons-tabs-modals
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/TokenGeneratorFormNew.tsx
  - src/components/GitHubConfig.tsx
  - src/components/FigmaConfig.tsx
  - src/components/GitHubDirectoryPicker.tsx
  - src/components/CollectionSelector.tsx
  - src/components/SharedCollectionHeader.tsx
  - src/components/SourceContextBar.tsx
autonomous: true
requirements:
  - SHADCN-FORMS

must_haves:
  truths:
    - "TokenGeneratorFormNew uses shadcn Input and Select for all form fields"
    - "TokenGeneratorFormNew uses shadcn Button for all action buttons"
    - "GitHubConfig, FigmaConfig use shadcn Input and Button"
    - "GitHubDirectoryPicker, CollectionSelector use shadcn Button and Input"
    - "No raw <button>, <input type='text'>, or <select> elements remain in any of these files"
  artifacts:
    - path: "src/components/TokenGeneratorFormNew.tsx"
      provides: "Token generator form with shadcn form controls"
      contains: "from '@/components/ui/input'"
    - path: "src/components/GitHubConfig.tsx"
      provides: "GitHub config form with shadcn controls"
      contains: "from '@/components/ui/input'"
    - path: "src/components/FigmaConfig.tsx"
      provides: "Figma config form with shadcn controls"
      contains: "from '@/components/ui/input'"
  key_links:
    - from: "src/components/TokenGeneratorFormNew.tsx"
      to: "src/components/ui/select.tsx"
      via: "import { Select, SelectTrigger, SelectContent, SelectItem, SelectValue }"
      pattern: "from.*components/ui/select"
    - from: "src/components/TokenGeneratorFormNew.tsx"
      to: "src/components/ui/input.tsx"
      via: "import { Input }"
      pattern: "from.*components/ui/input"
---

<objective>
Migrate the token generator form and all config/utility components to use shadcn Input, Select, and Button, completing the full shadcn migration across all src/ files.

Purpose: TokenGeneratorFormNew is the largest component (32KB) with the most form elements. GitHubConfig, FigmaConfig, GitHubDirectoryPicker, CollectionSelector, SharedCollectionHeader, and SourceContextBar complete the sweep.
Output: All 7 files use shadcn form controls. No raw `<button>`, `<input type="text">`, or `<select>` elements remain anywhere in src/ (except /dev-test sandbox).
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-update-ui-to-use-shadcn-components-for-common-elements-buttons-tabs-modals/01-01-SUMMARY.md
@src/components/TokenGeneratorFormNew.tsx
@src/components/GitHubConfig.tsx
@src/components/FigmaConfig.tsx
@src/components/GitHubDirectoryPicker.tsx
@src/components/CollectionSelector.tsx
@src/components/SharedCollectionHeader.tsx
@src/components/SourceContextBar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate GitHubConfig, FigmaConfig, GitHubDirectoryPicker, CollectionSelector, SharedCollectionHeader, SourceContextBar</name>
  <files>src/components/GitHubConfig.tsx, src/components/FigmaConfig.tsx, src/components/GitHubDirectoryPicker.tsx, src/components/CollectionSelector.tsx, src/components/SharedCollectionHeader.tsx, src/components/SourceContextBar.tsx</files>
  <action>
Read each file in full before editing. These are smaller components (fewer than 150 lines each). Apply this pattern to each:

Add shadcn imports as needed per file:
```typescript
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Select, SelectTrigger, SelectContent, SelectItem, SelectValue } from '@/components/ui/select';
```

**GitHubConfig.tsx:** Has inputs for GitHub token, owner, repo, branch. Replace all `<input>` with `<Input>`. Replace submit/save buttons with `<Button>`. Replace any `<select>` (branch dropdown) with shadcn `<Select>`:
```tsx
<Select value={value} onValueChange={onChange}>
  <SelectTrigger>
    <SelectValue placeholder="Select branch" />
  </SelectTrigger>
  <SelectContent>
    {options.map(opt => <SelectItem key={opt} value={opt}>{opt}</SelectItem>)}
  </SelectContent>
</Select>
```

**FigmaConfig.tsx:** Same pattern — inputs for Figma token/file ID, buttons for save/test actions.

**GitHubDirectoryPicker.tsx:** Has navigation buttons and possibly a path input. Replace `<button>` with `<Button variant="ghost">` for directory item rows. Replace any text inputs with `<Input>`.

**CollectionSelector.tsx:** Likely has a `<select>` dropdown for choosing collections. Replace with shadcn Select. May also have buttons.

**SharedCollectionHeader.tsx / SourceContextBar.tsx:** Likely have a few action buttons. Replace `<button>` with shadcn `<Button>` using appropriate variant.

For each `<select>` → shadcn Select migration:
- The shadcn Select uses `onValueChange` (string) not `onChange` (event). Update handlers:
  - Old: `onChange={(e) => setValue(e.target.value)}`
  - New: `onValueChange={(v) => setValue(v)}`

Do NOT change any state, props interfaces, or business logic. Only swap the HTML elements.
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && yarn build 2>&1 | grep -E "error|Error" | head -20</automated>
    <manual>Confirm each file has shadcn imports and no raw button/input/select elements</manual>
  </verify>
  <done>GitHubConfig.tsx, FigmaConfig.tsx, GitHubDirectoryPicker.tsx, CollectionSelector.tsx, SharedCollectionHeader.tsx, SourceContextBar.tsx all import from @/components/ui/. No raw button, input, or select elements remain. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate TokenGeneratorFormNew to shadcn Input, Select, and Button</name>
  <files>src/components/TokenGeneratorFormNew.tsx</files>
  <action>
Read the full file before editing (it is ~32KB / ~700+ lines — read it completely first).

Add shadcn imports at the top:
```typescript
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Select, SelectTrigger, SelectContent, SelectItem, SelectValue } from '@/components/ui/select';
```

Apply the migration in this order to avoid losing track in a large file:

1. **`<select>` elements → shadcn Select:**
   TokenGeneratorFormNew has dropdowns for token type, category, etc. For each `<select>`:
   - Replace `<select value={x} onChange={e => setX(e.target.value)}>` with the shadcn Select pattern using `onValueChange`.
   - Replace `<option value={v}>{label}</option>` with `<SelectItem value={v}>{label}</SelectItem>`.
   - Add `<SelectTrigger><SelectValue /></SelectTrigger>` inside the Select wrapper.

2. **`<input type="text">` / `<input type="number">` → shadcn Input:**
   There are approximately 9 input elements. Replace each `<input>` with `<Input>`. Preserve all props (value, onChange, placeholder, disabled, className, min, max, step for number inputs). The shadcn Input forwards all standard HTML input props.

3. **`<button>` → shadcn Button:**
   There are approximately 11 button elements. Map variants:
   - "Generate Tokens", "Save", "Load", primary CTA buttons → `variant="default"`
   - "Cancel", "Reset" → `variant="outline"`
   - "Delete", "Remove" type actions → `variant="destructive"`
   - Icon/inline utility buttons → `variant="ghost" size="sm"`

4. **Color-typed token input fields** (if any are in this form): Apply the same color picker rule from plan 01-02 — use `<input type="color">` for fields where the token type is 'color', shadcn `<Input>` for all others.

**Important:** This file is large. Work through it section by section. Do not skip any instance. After replacing all elements, verify the file still compiles.

Do NOT change:
- Dialog state (showJsonDialog, showSaveDialog, showLoadDialog, showExportFigmaDialog) — those dialogs were migrated in plan 01-03
- Any callback functions, form state, or token generation logic
- The imported dialog components (BuildTokensModal, SaveCollectionDialog, etc.) — those are already migrated separately
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && yarn build 2>&1 | grep -E "error|Error" | head -20</automated>
    <manual>Run `yarn dev`, visit http://localhost:3000/?tab=generate — form should render with shadcn-styled inputs, selects, and buttons</manual>
  </verify>
  <done>TokenGeneratorFormNew.tsx imports from @/components/ui/. No raw button, input, or select elements remain (grep -c returns 0). Build passes.</done>
</task>

</tasks>

<verification>
- `grep -rn "^import.*button\|^import.*input\|^import.*select" src/components/TokenGeneratorFormNew.tsx` shows shadcn imports
- `grep -c "&lt;button\|&lt;input\|&lt;select" src/components/TokenGeneratorFormNew.tsx` returns 0 (excluding color picker inputs which use `type="color"`)
- `yarn build` exits 0
- Final sweep: `grep -rn "&lt;button" src/components/ src/app/page.tsx | grep -v ".bak\|dev-test\|node_modules"` returns 0 matches
</verification>

<success_criteria>
All 7 components migrated. Zero raw `<button>`, `<input type="text">`, or `<select>` elements remain in src/ (except dev-test sandbox and native color picker inputs). Build passes. The full shadcn migration across Wave 2 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/01-update-ui-to-use-shadcn-components-for-common-elements-buttons-tabs-modals/01-04-SUMMARY.md`
</output>
