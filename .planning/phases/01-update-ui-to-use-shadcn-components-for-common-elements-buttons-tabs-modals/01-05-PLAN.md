---
phase: 01-update-ui-to-use-shadcn-components-for-common-elements-buttons-tabs-modals
plan: 05
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
  - 01-04
files_modified: []
autonomous: false
requirements:
  - SHADCN-VERIFY

must_haves:
  truths:
    - "The app renders correctly with shadcn components at runtime"
    - "Dialogs open and close without JavaScript errors"
    - "Tab switching works correctly"
    - "Color picker fields show a color swatch, not a text input"
    - "Form submission in the Generate tab still works"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification that the complete shadcn migration works end-to-end in the browser.

Purpose: Catches runtime issues that TypeScript compilation misses (hydration errors, missing CSS, dialog accessibility, event handler wiring).
Output: Human confirmation that all migrated UI elements work correctly.
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final build and raw element sweep</name>
  <files>src/components, src/app/page.tsx</files>
  <action>
Run a final verification sweep before handing off to human review:

1. Build the app:
   ```bash
   cd /Users/user/Dev/atui-tokens-manager && yarn build
   ```

2. Check for any remaining raw HTML elements that should have been migrated (excluding dev-test sandbox and color pickers):
   ```bash
   grep -rn "<button" src/components/ src/app/page.tsx | grep -v "AtuiDevTest\|\.bak"
   grep -rn "<input" src/components/ src/app/page.tsx | grep -v "AtuiDevTest\|\.bak\|type=\"color\"\|type='color'"
   grep -rn "<select" src/components/ src/app/page.tsx | grep -v "AtuiDevTest\|\.bak"
   ```

3. If any raw elements are found, fix them in-place before proceeding to the checkpoint.

4. Start the dev server in background and confirm it starts on port 3000 without errors.
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && yarn build 2>&1 | tail -5</automated>
    <manual>All grep sweeps return zero matches for raw HTML elements (excluding color pickers and dev-test)</manual>
  </verify>
  <done>yarn build exits 0. Zero remaining raw button/input/select elements outside dev-test. Dev server starts without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification</name>
  <files></files>
  <action>Pause for human to verify shadcn migration in the browser at http://localhost:3000 (dev server started in Task 1).</action>
  <verify>
    <automated>MISSING — visual verification only; human must approve</automated>
    <manual>
      Run `yarn dev` if not already running. Visit http://localhost:3000

      1. Tab switcher: Click between "View" and "Generate" tabs — should switch content. Tabs should have shadcn visual style (pill/underline indicator).

      2. Token table: In the View tab, load a collection. Check that:
         - Color tokens show a color swatch/picker input (not a text box)
         - Non-color tokens show styled text input fields

      3. Dialogs: Click "Export to Figma" or "Save Collection" — dialog should open with a centered overlay, shadcn styling, and close on X button or Escape key.

      4. Generate tab: Click the Generate tab. Check that:
         - Form inputs are styled (shadcn Input)
         - Dropdowns are styled (shadcn Select — click to open a popover, not a native OS select)
         - Buttons have shadcn styling

      5. GitHub config: Open GitHub settings/config if accessible — inputs and buttons should be shadcn styled.

      6. No visual regressions: Check that layout is not broken (items not misaligned, no missing content).

      7. Console errors: Open browser DevTools Console. No hydration warnings or "Cannot find module" errors.
    </manual>
  </verify>
  <done>Human approves: all UI elements render with shadcn styling, dialogs open/close correctly, tabs switch, color pickers show swatches, no console errors.</done>
  <what-built>
    Complete shadcn/ui migration across the entire app:
    - shadcn Button, Input, Select, Tabs, Dialog components installed in src/components/ui/
    - page.tsx: tab switcher uses shadcn Tabs, all action buttons use shadcn Button
    - TokenTable.tsx: text tokens use shadcn Input, color tokens show native color picker
    - All dialog components (BuildTokensModal, ExportToFigmaDialog, ImportFromFigmaDialog, JsonPreviewDialog, LoadCollectionDialog, SaveCollectionDialog, CollectionActions) use shadcn Dialog
    - TokenGeneratorFormNew, GitHubConfig, FigmaConfig, GitHubDirectoryPicker, CollectionSelector, SharedCollectionHeader, SourceContextBar all use shadcn form controls
  </what-built>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human confirms:
- All UI elements render with shadcn styling
- Dialogs open/close correctly with Escape and X button
- Tabs switch correctly
- Color pickers show color swatches
- No console errors or hydration warnings
</verification>

<success_criteria>
Human approves the visual verification. Phase 1 shadcn migration is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/01-update-ui-to-use-shadcn-components-for-common-elements-buttons-tabs-modals/01-05-SUMMARY.md`
</output>
