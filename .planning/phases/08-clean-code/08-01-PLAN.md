---
phase: 08-clean-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/TokenGeneratorForm.tsx
  - src/components/TokenGeneratorFormNew.tsx
  - src/app/collections/[id]/tokens/page.tsx
  - src/app/generate/page.tsx
  - src/app/settings/page.tsx
  - src/app/configuration/page.tsx
  - src/app/collections.tsx
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02

must_haves:
  truths:
    - "Old TokenGeneratorForm.tsx (legacy) is deleted; only one form component exists named TokenGeneratorForm.tsx"
    - "All import sites reference TokenGeneratorForm (not TokenGeneratorFormNew)"
    - "Legacy route directories (generate/, settings/, configuration/) are deleted"
    - "src/app/collections.tsx is confirmed dead and deleted (if confirmed unused)"
    - "npm run build produces no module-not-found errors from dead code removal"
  artifacts:
    - path: "src/components/TokenGeneratorForm.tsx"
      provides: "Renamed form (was TokenGeneratorFormNew.tsx)"
    - path: "src/app/collections/[id]/tokens/page.tsx"
      provides: "Updated import site: TokenGeneratorFormNew → TokenGeneratorForm"
  key_links:
    - from: "src/app/collections/[id]/tokens/page.tsx"
      to: "src/components/TokenGeneratorForm.tsx"
      via: "import statement"
      pattern: "import.*TokenGeneratorForm.*from.*@/components/TokenGeneratorForm"
---

<objective>
Remove confirmed dead code and rename the active form component from `TokenGeneratorFormNew` to `TokenGeneratorForm`.

Purpose: Eliminate confusion from two similarly-named form files; remove stale app route directories that clutter the Next.js router.
Output: One form component with the canonical name, three legacy route directories deleted, import sites updated.
</objective>

<execution_context>
@/home/rikis/.claude/get-shit-done/workflows/execute-plan.md
@/home/rikis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-clean-code/08-CONTEXT.md

<interfaces>
<!-- Current import sites for TokenGeneratorFormNew (must be updated in this plan) -->

src/app/collections/[id]/tokens/page.tsx line 9:
```typescript
import { TokenGeneratorFormNew } from '@/components/TokenGeneratorFormNew';
```
Used at line 408 as `<TokenGeneratorFormNew ...`

<!-- Legacy routes confirmed dead: -->
src/app/generate/page.tsx  — just a redirect stub: redirect('/?tab=generate')
src/app/settings/page.tsx  — legacy settings page using CollectionContext
src/app/configuration/page.tsx — legacy configuration page using CollectionContext
src/app/collections.tsx    — audit: if dead (no router matches it), delete it

<!-- DB factory is LIVE — do NOT delete or modify get-repository.ts -->
src/lib/db/get-repository.ts — confirmed used by all API routes via `import { getRepository }`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old TokenGeneratorForm.tsx and rename New → canonical</name>
  <files>src/components/TokenGeneratorForm.tsx, src/components/TokenGeneratorFormNew.tsx, src/app/collections/[id]/tokens/page.tsx</files>
  <action>
    1. Delete `src/components/TokenGeneratorForm.tsx` (the old legacy form — the one that exports `export function TokenGeneratorForm()` with the original legacy implementation). Confirm it has no live import sites first with: `grep -rn "from '@/components/TokenGeneratorForm'" src/` — if results only point to the component itself (no external consumers), safe to delete.
    2. Rename `src/components/TokenGeneratorFormNew.tsx` → `src/components/TokenGeneratorForm.tsx`. Inside the file, rename the export: `TokenGeneratorFormNew` → `TokenGeneratorForm` and the interface `TokenGeneratorFormNewProps` → `TokenGeneratorFormProps`.
    3. In `src/app/collections/[id]/tokens/page.tsx`:
       - Update the import: `import { TokenGeneratorFormNew } from '@/components/TokenGeneratorFormNew'` → `import { TokenGeneratorForm } from '@/components/TokenGeneratorForm'`
       - Update the JSX usage: `<TokenGeneratorFormNew` → `<TokenGeneratorForm` (and closing tag if applicable)
    4. Check for any other remaining references to `TokenGeneratorFormNew`: `grep -rn "TokenGeneratorFormNew" src/` — fix any found.
  </action>
  <verify>
    <automated>grep -rn "TokenGeneratorFormNew" /home/rikis/dev/tokens-manager/src/ --include="*.ts" --include="*.tsx" | wc -l</automated>
  </verify>
  <done>Output is 0 — no remaining references to TokenGeneratorFormNew anywhere in src/</done>
</task>

<task type="auto">
  <name>Task 2: Delete legacy app routes and audit collections.tsx</name>
  <files>src/app/generate/page.tsx, src/app/settings/page.tsx, src/app/configuration/page.tsx, src/app/collections.tsx</files>
  <action>
    1. Delete `src/app/generate/page.tsx` — confirmed to be a redirect stub only.
    2. Delete `src/app/settings/page.tsx` — confirmed legacy settings page (DatabaseConfig component available elsewhere via proper route).
    3. Delete `src/app/configuration/page.tsx` — confirmed legacy configuration page.
    4. Remove the empty directories left behind: `src/app/generate/`, `src/app/settings/`, `src/app/configuration/`.
    5. Audit `src/app/collections.tsx`: In Next.js App Router, `src/app/collections.tsx` would conflict with `src/app/collections/` (directory). Check if it is referenced anywhere: `grep -rn "collections.tsx\|app/collections'" src/ app/` and check if Next.js is even aware of it as a route. If it has no content (confirmed empty from earlier) or is simply dead, delete it. If it has real content that's still needed, keep and note in SUMMARY.

    After deletions, run `npx tsc --noEmit 2>&1 | grep -i "cannot find module\|no such file"` to confirm no import of deleted files remains.
  </action>
  <verify>
    <automated>ls /home/rikis/dev/tokens-manager/src/app/generate 2>/dev/null && echo "EXISTS" || echo "DELETED"</automated>
  </verify>
  <done>generate/, settings/, configuration/ directories no longer exist; collections.tsx deleted or explicitly kept with documented reason</done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit 2>&1` — should not show any new errors introduced by this plan. Errors from Plan 02 (TS fixes) may still appear — that is expected.
Run `grep -rn "TokenGeneratorFormNew" src/` — should return zero results.
</verification>

<success_criteria>
- Dead form component deleted; only TokenGeneratorForm.tsx remains in src/components/
- All import sites updated; zero references to TokenGeneratorFormNew
- Legacy app routes (generate, settings, configuration) deleted
- TypeScript build produces no new "cannot find module" errors from this plan's changes
</success_criteria>

<output>
After completion, create `.planning/phases/08-clean-code/08-01-SUMMARY.md` documenting:
- Which files were deleted and why
- The rename performed and all import sites updated
- Any decision made about collections.tsx (kept or deleted and why)
</output>
