---
phase: 08-clean-code
plan: 04
type: execute
wave: 3
depends_on:
  - "08-03"
files_modified:
  - src/utils/token.utils.ts
  - src/utils/tree.utils.ts
  - src/utils/tokenUpdater.ts
  - src/utils/index.ts
  - src/services/token.service.ts
  - src/lib/db/get-repository.ts
  - src/components/tokens/TokenGeneratorForm.tsx
  - src/components/collections/CollectionActions.tsx
  - src/components/layout/LayoutShell.tsx
  - src/components/layout/OrgHeader.tsx
  - .planning/phases/08-clean-code/REFACTOR-SUGGESTIONS.md
autonomous: true
requirements:
  - CLEAN-05
  - CLEAN-06

must_haves:
  truths:
    - "src/utils/ functions have zero React or Next.js imports — they are pure, framework-agnostic"
    - "Non-rendering logic extracted from components goes into src/utils/ (pure transforms) or src/services/ (I/O-bound)"
    - "get-repository.ts is clearly documented as the live factory (not dead code)"
    - "A REFACTOR-SUGGESTIONS.md file exists with out-of-scope ideas captured for future phases"
    - "npx tsc --noEmit exits with zero errors"
  artifacts:
    - path: "src/utils/index.ts"
      provides: "Barrel export for all utils — confirms no React/Next.js imports in any util"
    - path: ".planning/phases/08-clean-code/REFACTOR-SUGGESTIONS.md"
      provides: "Captured out-of-scope refactor ideas (do not act on them)"
    - path: "src/lib/db/get-repository.ts"
      provides: "Documented factory — JSDoc comment clarifying it is the live entry point"
  key_links:
    - from: "src/components/tokens/TokenGeneratorForm.tsx"
      to: "src/utils/"
      via: "extracted utility functions"
      pattern: "import.*from.*@/utils"
---

<objective>
Apply a Single Responsibility Principle (SRP) pass across all components: extract non-rendering logic (data transforms, API calls inline in components) into `src/utils/` (pure) or `src/services/` (I/O-bound). Document the `get-repository.ts` factory. Capture out-of-scope refactor ideas for future phases.

Purpose: Components should render; business logic and data transforms should live in testable, framework-agnostic functions.
Output: Cleaner components with extracted logic, documented DB factory, and a REFACTOR-SUGGESTIONS.md for future use.
</objective>

<execution_context>
@/home/rikis/.claude/get-shit-done/workflows/execute-plan.md
@/home/rikis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-clean-code/08-CONTEXT.md
@.planning/phases/08-clean-code/08-03-SUMMARY.md

<interfaces>
<!-- SRP extraction rules (from user decisions): -->
<!-- src/utils/  → pure functions, zero React/Next.js imports, framework-agnostic transforms -->
<!-- src/services/ → I/O-bound operations: API calls, DB operations, file system -->

<!-- Existing utils (already structured — add to, don't replace): -->
src/utils/token.utils.ts    — token data transforms
src/utils/tree.utils.ts     — tree parsing/traversal utilities
src/utils/tokenUpdater.ts   — token mutation helpers
src/utils/ui.utils.ts       — UI helpers (toast creation, etc.)
src/utils/validation.utils.ts — validation functions

<!-- Existing services (already structured — add to, don't replace): -->
src/services/figma.service.ts
src/services/file.service.ts
src/services/github.service.ts
src/services/style-dictionary.service.ts
src/services/token.service.ts

<!-- DB factory (confirmed live — used by all API routes): -->
src/lib/db/get-repository.ts — factory function, exports getRepository() and invalidateRepository()
All API routes under src/app/api/ import { getRepository } from this file.

<!-- SRP audit focus areas (per CONTEXT.md): -->
<!-- 1. TokenGeneratorForm (1394 lines) — likely has inline data transforms and API calls -->
<!-- 2. Inline fetch() calls in components that could be in services -->
<!-- 3. Data reduction/mapping logic inline in useEffects -->
<!-- The goal is to move OBVIOUSLY misplaced logic first, then do a full audit -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and document get-repository, verify utils purity</name>
  <files>src/lib/db/get-repository.ts, src/utils/index.ts, src/utils/token.utils.ts, src/utils/tree.utils.ts, src/utils/tokenUpdater.ts</files>
  <action>
    **Step 1 — Document get-repository.ts:**
    The factory is confirmed live (all API routes use it). Add/update the JSDoc comment at the top of `get-repository.ts` to clearly state:
    - This is the single entry point for database access
    - All API routes MUST use this factory (not import mongo-repository directly)
    - Provider is read from db config; result is cached per-process
    - To switch providers, update the config and restart the server
    Example header comment to add above the export:
    ```typescript
    /**
     * Database repository factory — the single entry point for all database access.
     *
     * All API routes MUST use getRepository() from this file.
     * Do NOT import MongoCollectionRepository or SupabaseCollectionRepository directly.
     *
     * Provider selection: reads from db config (set via Settings UI).
     * Result is cached per-process; restart server after changing provider.
     */
    ```

    **Step 2 — Verify utils purity:**
    Check each file in src/utils/ for React or Next.js imports:
    ```bash
    grep -n "from 'react'\|from 'next/" src/utils/*.ts
    ```
    If any utils file imports React or Next.js:
    - If the import is for a React type only (e.g., `import type { ReactNode }`), move the logic to a component or service instead
    - Pure functions must not have React/Next.js imports — move offending logic to a more appropriate location
    - Update src/utils/index.ts (barrel) to confirm all exports are framework-agnostic

    **Step 3 — Document findings:**
    Record in the SUMMARY what was found (clean or needed fixing).
  </action>
  <verify>
    <automated>grep -rn "from 'react'\|from 'next/" /home/rikis/dev/tokens-manager/src/utils/*.ts 2>/dev/null | wc -l</automated>
  </verify>
  <done>Output is 0 — no React or Next.js imports in any utils file</done>
</task>

<task type="auto">
  <name>Task 2: SRP pass on components — extract non-rendering logic, produce suggestions list</name>
  <files>
    src/components/tokens/TokenGeneratorForm.tsx,
    src/components/collections/CollectionActions.tsx,
    src/components/layout/LayoutShell.tsx,
    src/components/layout/OrgHeader.tsx,
    src/utils/token.utils.ts,
    src/services/token.service.ts,
    .planning/phases/08-clean-code/REFACTOR-SUGGESTIONS.md
  </files>
  <action>
    **Step 1 — Move obviously misplaced logic:**
    Scan each component for:
    a) Inline data transforms (`.map`, `.reduce`, `.filter` pipelines not directly tied to JSX) → candidate for src/utils/
    b) Direct `fetch()` calls inside components that could live in src/services/
    c) Business rules embedded in event handlers

    Priority components to audit (largest/most complex first):
    - `src/components/tokens/TokenGeneratorForm.tsx` (1394 lines — high likelihood of extractable logic)
    - `src/components/collections/CollectionActions.tsx`
    - `src/components/layout/LayoutShell.tsx`
    - `src/components/layout/OrgHeader.tsx`

    For each piece of extractable logic found:
    - If it's a pure data transform (no I/O, no React hooks): extract to appropriate file in src/utils/
    - If it's an API call (fetch to /api/): check if src/services/token.service.ts or similar already handles it; if so, replace inline fetch with service call; if not, add a service function
    - Update the component to import and call the extracted function

    Rules:
    - Only extract logic that is OBVIOUSLY misplaced — don't over-engineer
    - Do not break existing functionality
    - Extracted utils must remain pure (no React imports)
    - After extraction, component's JSX responsibilities should be clearer

    **Step 2 — Full audit, produce suggestions list:**
    After extracting the obvious cases, do a full read-through of remaining components and produce `.planning/phases/08-clean-code/REFACTOR-SUGGESTIONS.md` with the format:
    ```markdown
    # Refactor Suggestions (Phase 8 Audit)

    These are OUT OF SCOPE for Phase 8 — captured for future phases.
    Do not implement these in the current phase.

    ## Suggestions

    | Component | Issue | Suggested Fix | Priority |
    |-----------|-------|---------------|----------|
    | TokenGeneratorForm.tsx | 1394 lines — should be split into sub-components | Extract TokenRow, GroupSection as sub-components | Medium |
    | ... | ... | ... | ... |
    ```

    Capture ideas; do NOT act on suggestions — just document them.

    **Step 3 — Run tsc to confirm no regressions:**
    `npx tsc --noEmit` — should remain at zero errors.
  </action>
  <verify>
    <automated>ls /home/rikis/dev/tokens-manager/.planning/phases/08-clean-code/REFACTOR-SUGGESTIONS.md && echo "EXISTS"</automated>
  </verify>
  <done>REFACTOR-SUGGESTIONS.md exists with at least 3 captured suggestions; tsc outputs zero errors</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` — zero errors.
`grep -rn "from 'react'\|from 'next/" src/utils/` — zero results.
`cat .planning/phases/08-clean-code/REFACTOR-SUGGESTIONS.md` — file exists and contains a markdown table of suggestions.
</verification>

<success_criteria>
- get-repository.ts has clear documentation as the live DB factory entry point
- Utils are framework-agnostic (zero React/Next.js imports in src/utils/)
- At least some non-rendering logic extracted from the largest components into utils/services
- REFACTOR-SUGGESTIONS.md created with out-of-scope ideas for future phases
- TypeScript remains clean (zero errors)
</success_criteria>

<output>
After completion, create `.planning/phases/08-clean-code/08-04-SUMMARY.md` documenting:
- What logic was extracted and where it was moved
- Which components still have embedded logic and why (recorded in REFACTOR-SUGGESTIONS.md)
- The full list of entries in REFACTOR-SUGGESTIONS.md
- Final tsc status
</output>
