---
phase: 08-clean-code
plan: 03
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/components/collections/index.ts
  - src/components/tokens/index.ts
  - src/components/layout/index.ts
  - src/components/figma/index.ts
  - src/components/github/index.ts
  - src/components/dev/index.ts
  - src/components/CollectionCard.tsx
  - src/components/CollectionActions.tsx
  - src/components/collectionHeader.tsx
  - src/components/CollectionHeader.tsx
  - src/components/CollectionLayoutClient.tsx
  - src/components/CollectionSelector.tsx
  - src/components/CollectionSidebar.tsx
  - src/components/NewCollectionDialog.tsx
  - src/components/DeleteCollectionDialog.tsx
  - src/components/LoadCollectionDialog.tsx
  - src/components/SaveCollectionDialog.tsx
  - src/components/TokenTable.tsx
  - src/components/TokenGeneratorForm.tsx
  - src/components/TokenGroupTree.tsx
  - src/components/GroupBreadcrumb.tsx
  - src/components/TokenReferencePicker.tsx
  - src/components/TokenGeneratorDocs.tsx
  - src/components/LayoutShell.tsx
  - src/components/AppSidebar.tsx
  - src/components/AppHeader.tsx
  - src/components/OrgSidebar.tsx
  - src/components/OrgHeader.tsx
  - src/components/SharedCollectionHeader.tsx
  - src/components/SourceContextBar.tsx
  - src/components/LoadingIndicator.tsx
  - src/components/ToastNotification.tsx
  - src/components/FigmaConfig.tsx
  - src/components/ImportFromFigmaDialog.tsx
  - src/components/ExportToFigmaDialog.tsx
  - src/components/GitHubConfig.tsx
  - src/components/GitHubDirectoryPicker.tsx
  - src/components/AtuiDevTest.tsx
  - src/components/BuildTokensPanel.tsx
  - src/components/BuildTokensModal.tsx
  - src/components/JsonPreviewDialog.tsx
  - src/components/DatabaseConfig.tsx
  - src/app/collections/[id]/tokens/page.tsx
  - src/app/collections/[id]/layout.tsx
  - src/app/collections/page.tsx
  - src/app/collections/[id]/config/page.tsx
  - src/app/layout.tsx
autonomous: true
requirements:
  - CLEAN-04

must_haves:
  truths:
    - "src/components/ contains only subdirectory folders (collections/, tokens/, layout/, figma/, github/, dev/, ui/, graph/) — no loose component .tsx files remain at the top level"
    - "Each feature domain folder has an index.ts barrel export re-exporting all components in that folder"
    - "collectionHeader.tsx is renamed to CollectionHeader.tsx (PascalCase)"
    - "All import sites in src/app/ and src/components/ updated to import from the new paths"
    - "npx tsc --noEmit produces zero errors after reorganization"
  artifacts:
    - path: "src/components/collections/index.ts"
      provides: "Barrel exports for all collection-domain components"
      exports:
        - CollectionCard
        - CollectionActions
        - CollectionHeader
        - CollectionLayoutClient
        - CollectionSelector
        - CollectionSidebar
        - NewCollectionDialog
        - DeleteCollectionDialog
        - LoadCollectionDialog
        - SaveCollectionDialog
    - path: "src/components/tokens/index.ts"
      provides: "Barrel exports for token-domain components"
      exports:
        - TokenTable
        - TokenGeneratorForm
        - TokenGroupTree
        - GroupBreadcrumb
        - TokenReferencePicker
        - TokenGeneratorDocs
    - path: "src/components/layout/index.ts"
      provides: "Barrel exports for layout components"
    - path: "src/components/figma/index.ts"
      provides: "Barrel exports for Figma components"
    - path: "src/components/github/index.ts"
      provides: "Barrel exports for GitHub components"
    - path: "src/components/dev/index.ts"
      provides: "Barrel exports for dev/sandbox components"
  key_links:
    - from: "src/app/collections/[id]/tokens/page.tsx"
      to: "src/components/tokens/TokenGeneratorForm.tsx"
      via: "import via barrel"
      pattern: "import.*TokenGeneratorForm.*from.*@/components/tokens"
    - from: "src/app/layout.tsx"
      to: "src/components/layout/LayoutShell.tsx"
      via: "import via barrel"
      pattern: "import.*LayoutShell.*from.*@/components/layout"
---

<objective>
Reorganize `src/components/` into feature domain subdirectories with barrel exports, and fix the `collectionHeader.tsx` PascalCase naming.

Purpose: Replace a flat 35+ file directory with a navigable feature-domain structure. Barrel exports mean import sites use consistent paths and consumers don't care about internal file locations.
Output: Six new domain folders (collections/, tokens/, layout/, figma/, github/, dev/) each with an index.ts, all components moved, all import sites updated.
</objective>

<execution_context>
@/home/rikis/.claude/get-shit-done/workflows/execute-plan.md
@/home/rikis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-clean-code/08-CONTEXT.md

<interfaces>
<!-- Folder assignment (per user decisions in CONTEXT.md): -->

collections/ :
  CollectionCard, CollectionActions, CollectionHeader (renamed from collectionHeader),
  CollectionLayoutClient, CollectionSelector, CollectionSidebar,
  NewCollectionDialog, DeleteCollectionDialog, LoadCollectionDialog, SaveCollectionDialog

tokens/ :
  TokenTable, TokenGeneratorForm (renamed from TokenGeneratorFormNew in Plan 01),
  TokenGroupTree, GroupBreadcrumb, TokenReferencePicker, TokenGeneratorDocs

layout/ :
  LayoutShell, AppSidebar, AppHeader, OrgSidebar, OrgHeader,
  SharedCollectionHeader, SourceContextBar, LoadingIndicator, ToastNotification

figma/ :
  FigmaConfig, ImportFromFigmaDialog, ExportToFigmaDialog

github/ :
  GitHubConfig, GitHubDirectoryPicker

dev/ :
  AtuiDevTest, BuildTokensPanel, BuildTokensModal, JsonPreviewDialog, DatabaseConfig

ui/ :  ← KEEP AS-IS (shadcn primitives, already in src/components/ui/)
graph/ : ← KEEP AS-IS (already grouped)

<!-- IMPORTANT: DatabaseConfig is user-confirmed in dev/ per CONTEXT.md decisions -->

<!-- Current import sites that must be updated after moves: -->

src/app/collections/[id]/tokens/page.tsx imports from @/components/ (flat):
  ToastNotification, SaveCollectionDialog, TokenGeneratorForm (after Plan 01 rename),
  TokenGeneratorDocs, SourceContextBar, ImportFromFigmaDialog, CollectionActions,
  TokenGroupTree, GroupBreadcrumb, TokenGraphPanel (in graph/ — keep as-is)

src/app/collections/[id]/layout.tsx:
  CollectionLayoutClient

src/app/collections/page.tsx:
  CollectionCard, DeleteCollectionDialog, NewCollectionDialog

src/app/collections/[id]/config/page.tsx:
  BuildTokensPanel

src/app/layout.tsx (root layout):
  Check if it imports LayoutShell or any components — update if so

src/components/LayoutShell.tsx:
  AppHeader, AppSidebar, OrgHeader, OrgSidebar (all moving to layout/)
  After moving, LayoutShell itself is in layout/ — update relative/absolute imports

src/components/AppHeader.tsx:
  CollectionSelector (moving to collections/)

src/components/CollectionLayoutClient.tsx:
  CollectionSidebar (moving to collections/)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create domain folders and move components</name>
  <files>
    src/components/collections/CollectionCard.tsx,
    src/components/collections/CollectionActions.tsx,
    src/components/collections/CollectionHeader.tsx,
    src/components/collections/CollectionLayoutClient.tsx,
    src/components/collections/CollectionSelector.tsx,
    src/components/collections/CollectionSidebar.tsx,
    src/components/collections/NewCollectionDialog.tsx,
    src/components/collections/DeleteCollectionDialog.tsx,
    src/components/collections/LoadCollectionDialog.tsx,
    src/components/collections/SaveCollectionDialog.tsx,
    src/components/tokens/TokenTable.tsx,
    src/components/tokens/TokenGeneratorForm.tsx,
    src/components/tokens/TokenGroupTree.tsx,
    src/components/tokens/GroupBreadcrumb.tsx,
    src/components/tokens/TokenReferencePicker.tsx,
    src/components/tokens/TokenGeneratorDocs.tsx,
    src/components/layout/LayoutShell.tsx,
    src/components/layout/AppSidebar.tsx,
    src/components/layout/AppHeader.tsx,
    src/components/layout/OrgSidebar.tsx,
    src/components/layout/OrgHeader.tsx,
    src/components/layout/SharedCollectionHeader.tsx,
    src/components/layout/SourceContextBar.tsx,
    src/components/layout/LoadingIndicator.tsx,
    src/components/layout/ToastNotification.tsx,
    src/components/figma/FigmaConfig.tsx,
    src/components/figma/ImportFromFigmaDialog.tsx,
    src/components/figma/ExportToFigmaDialog.tsx,
    src/components/github/GitHubConfig.tsx,
    src/components/github/GitHubDirectoryPicker.tsx,
    src/components/dev/AtuiDevTest.tsx,
    src/components/dev/BuildTokensPanel.tsx,
    src/components/dev/BuildTokensModal.tsx,
    src/components/dev/JsonPreviewDialog.tsx,
    src/components/dev/DatabaseConfig.tsx
  </files>
  <action>
    Create the six domain subdirectories and move all components into them.

    Use `mkdir -p` for each directory:
    - src/components/collections/
    - src/components/tokens/
    - src/components/layout/
    - src/components/figma/
    - src/components/github/
    - src/components/dev/

    Move each component using `mv`:
    - `src/components/collectionHeader.tsx` → `src/components/collections/CollectionHeader.tsx` (rename to PascalCase during move)
    - All other components: `mv src/components/[ComponentName].tsx src/components/[domain]/[ComponentName].tsx`

    IMPORTANT: After moving, update any `@/components/` imports WITHIN the moved files themselves that reference other components (e.g., LayoutShell imports AppHeader — once both are in layout/, update the imports).

    Within-folder cross-imports (now in same folder):
    - `src/components/layout/LayoutShell.tsx` imports AppHeader, AppSidebar, OrgHeader, OrgSidebar — update to relative imports: `import { AppHeader } from './AppHeader'` etc., OR keep using `@/components/layout/AppHeader` absolute path.
    - `src/components/layout/AppHeader.tsx` imports CollectionSelector — update to `@/components/collections/CollectionSelector`
    - `src/components/collections/CollectionLayoutClient.tsx` imports CollectionSidebar — update to `@/components/collections/CollectionSidebar` or relative `./CollectionSidebar`

    Use `@/components/[domain]/[ComponentName]` pattern for all cross-domain imports (absolute alias paths). Use relative imports only for same-folder references if preferred.

    After all moves, confirm the flat `src/components/` level has no .tsx files remaining (only the ui/ and graph/ subdirectories plus the new domain directories):
    `ls src/components/*.tsx 2>&1` — should return "No such file or directory".
  </action>
  <verify>
    <automated>ls /home/rikis/dev/tokens-manager/src/components/*.tsx 2>&1 | grep -c "\.tsx$"</automated>
  </verify>
  <done>Output is 0 — no .tsx files remain at the flat src/components/ level</done>
</task>

<task type="auto">
  <name>Task 2: Create barrel index.ts files and update all app import sites</name>
  <files>
    src/components/collections/index.ts,
    src/components/tokens/index.ts,
    src/components/layout/index.ts,
    src/components/figma/index.ts,
    src/components/github/index.ts,
    src/components/dev/index.ts,
    src/app/collections/[id]/tokens/page.tsx,
    src/app/collections/[id]/layout.tsx,
    src/app/collections/page.tsx,
    src/app/collections/[id]/config/page.tsx,
    src/app/layout.tsx
  </files>
  <action>
    **Step 1: Create barrel index.ts for each domain folder.**
    Each index.ts re-exports all components in that folder using named exports:
    ```typescript
    // Example: src/components/collections/index.ts
    export { CollectionCard } from './CollectionCard';
    export { CollectionActions } from './CollectionActions';
    // ... etc
    ```
    Create all six barrel files for: collections/, tokens/, layout/, figma/, github/, dev/.

    **Step 2: Update import sites in src/app/ pages.**
    Find all files in src/app/ that import from @/components/* (flat) and update them:

    `src/app/collections/[id]/tokens/page.tsx`:
    - `@/components/ToastNotification` → `@/components/layout/ToastNotification`
    - `@/components/SaveCollectionDialog` → `@/components/collections/SaveCollectionDialog`
    - `@/components/TokenGeneratorForm` → `@/components/tokens/TokenGeneratorForm`
    - `@/components/TokenGeneratorDocs` → `@/components/tokens/TokenGeneratorDocs`
    - `@/components/SourceContextBar` → `@/components/layout/SourceContextBar`
    - `@/components/ImportFromFigmaDialog` → `@/components/figma/ImportFromFigmaDialog`
    - `@/components/CollectionActions` → `@/components/collections/CollectionActions`
    - `@/components/TokenGroupTree` → `@/components/tokens/TokenGroupTree`
    - `@/components/GroupBreadcrumb` → `@/components/tokens/GroupBreadcrumb`
    - graph imports (`@/components/graph/`) — keep as-is

    `src/app/collections/[id]/layout.tsx`:
    - `@/components/CollectionLayoutClient` → `@/components/collections/CollectionLayoutClient`

    `src/app/collections/page.tsx`:
    - `@/components/CollectionCard` → `@/components/collections/CollectionCard`
    - `@/components/DeleteCollectionDialog` → `@/components/collections/DeleteCollectionDialog`
    - `@/components/NewCollectionDialog` → `@/components/collections/NewCollectionDialog`

    `src/app/collections/[id]/config/page.tsx`:
    - `@/components/BuildTokensPanel` → `@/components/dev/BuildTokensPanel`

    `src/app/layout.tsx` (root layout):
    - Check and update any LayoutShell or layout component imports → `@/components/layout/LayoutShell`

    **Step 3: Run tsc to catch any remaining stale imports.**
    `npx tsc --noEmit 2>&1 | grep "Cannot find module\|No exported member"` — fix any remaining broken imports found.
  </action>
  <verify>
    <automated>cd /home/rikis/dev/tokens-manager && npx tsc --noEmit 2>&1 | grep -c "Cannot find module\|No exported member\|Could not find"</automated>
  </verify>
  <done>Output is 0 — no missing-module or missing-export errors</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` — zero errors after reorganization.
`ls src/components/*.tsx` — returns no files (all components in domain subdirectories).
`ls src/components/collections/ src/components/tokens/ src/components/layout/ src/components/figma/ src/components/github/ src/components/dev/` — all six directories exist with index.ts and component files.
`grep -rn "collectionHeader" src/` — returns zero results (no lowercase-c reference remains).
</verification>

<success_criteria>
- All 35 components organized into 6 feature domain subdirectories
- Each domain directory has an index.ts barrel export
- collectionHeader.tsx renamed to CollectionHeader.tsx
- All import sites in src/app/ updated to new paths
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-clean-code/08-03-SUMMARY.md` documenting:
- The domain folders created and which components went where
- Any placement decisions made at Claude's discretion (e.g., DatabaseConfig in dev/)
- Cross-import updates made within moved files
- Final tsc status
</output>
