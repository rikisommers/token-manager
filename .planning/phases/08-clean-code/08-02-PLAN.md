---
phase: 08-clean-code
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/collections/route.ts
  - src/app/api/database/test/route.ts
  - src/app/collections/[id]/tokens/page.tsx
  - src/components/AtuiDevTest.tsx
autonomous: true
requirements:
  - CLEAN-03

must_haves:
  truths:
    - "npx tsc --noEmit exits with zero errors"
    - "No @ts-ignore or 'as any' casts introduced to silence errors"
    - "AtuiDevTest.tsx uses shadcn components instead of the broken ATUI stencil loader import"
  artifacts:
    - path: "src/app/api/collections/route.ts"
      provides: "POST body sourceMetadata typed as ISourceMetadata with all required fields"
    - path: "src/app/api/database/test/route.ts"
      provides: "testConnection typed as Connection (not typeof mongoose)"
    - path: "src/app/collections/[id]/tokens/page.tsx"
      provides: "onTokensChange callback accepts null (or TokenGeneratorForm signature updated)"
    - path: "src/components/AtuiDevTest.tsx"
      provides: "Shadcn-based dev test component with no broken ATUI loader import"
  key_links:
    - from: "src/app/api/collections/route.ts"
      to: "src/types/collection.types.ts"
      via: "ISourceMetadata import"
      pattern: "import.*ISourceMetadata.*from.*collection.types"
    - from: "src/app/api/database/test/route.ts"
      to: "mongoose"
      via: "Connection type import"
      pattern: "import.*Connection.*from.*mongoose"
---

<objective>
Fix all TypeScript errors in the codebase without using suppressors (`@ts-ignore`, `as any`).

Purpose: Clean TypeScript build is a prerequisite for a maintainable codebase and ensures type safety is not silently bypassed.
Output: Zero TypeScript errors, AtuiDevTest migrated to shadcn.
</objective>

<execution_context>
@/home/rikis/.claude/get-shit-done/workflows/execute-plan.md
@/home/rikis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-clean-code/08-CONTEXT.md

<interfaces>
<!-- Error 1: src/app/api/collections/route.ts(56,7) -->
<!-- Type '{ repo: string | null; branch: string | null; path: string | null; } | null' -->
<!-- is not assignable to type 'ISourceMetadata | null | undefined' -->
<!-- Missing fields: type, figmaFileKey, figmaCollectionId -->
<!-- Fix: Widen the POST body type to accept full ISourceMetadata shape -->

From src/types/collection.types.ts:
```typescript
export interface ISourceMetadata {
  repo: string | null;
  branch: string | null;
  path: string | null;
  type: 'github' | 'figma' | null;
  figmaFileKey: string | null;
  figmaCollectionId: string | null;
}
```

The POST body at line 33 only declares:
```typescript
sourceMetadata?: { repo: string | null; branch: string | null; path: string | null } | null;
```
Fix: Update the inline type to match ISourceMetadata fully (import and use the type, or expand the inline type to include all fields with correct types).

<!-- Error 2: src/app/api/database/test/route.ts(27,5) -->
<!-- Type 'Connection' is missing properties from 'typeof mongoose' -->
<!-- testConnection is typed as `let testConnection: typeof mongoose | null = null` -->
<!-- but mongoose.createConnection() returns Connection, not typeof mongoose -->
<!-- Fix: type testConnection as Connection | null and import Connection from mongoose -->

From mongoose:
```typescript
import mongoose, { Connection } from 'mongoose';
// mongoose.createConnection(...).asPromise() returns Promise<Connection>
// Connection has: .host, .port, .db, .close()
```

<!-- Error 3 & 4: src/app/api/database/test/route.ts(34-36, 62) -->
<!-- testConnection.host/.port/.db/.close() don't exist on typeof mongoose -->
<!-- Fixed automatically once testConnection is typed as Connection | null -->
<!-- But also: testConnection is possibly null at line 34 — need null check or optional chaining -->

<!-- Error 5: src/app/collections/[id]/tokens/page.tsx(411,19) -->
<!-- handleTokensChange signature: (tokens: Record<string, unknown>) => void -->
<!-- but TokenGeneratorFormNew onTokensChange prop expects: (tokens: Record<string, unknown> | null, namespace: string, collectionName: string) => void -->
<!-- Fix: Update handleTokensChange to accept (tokens: Record<string, unknown> | null, namespace: string, collectionName: string) -->
<!-- and handle null case explicitly (e.g., setRawCollectionTokens(tokens ?? {}) or setRawCollectionTokens(null)) -->

<!-- Error 6: src/components/AtuiDevTest.tsx(21,12) -->
<!-- Cannot find module '@alliedtelesis-labs-nz/atui-components-stencil/loader' -->
<!-- Fix: Replace ATUI stencil loader usage with shadcn equivalent UI -->
<!-- AtuiDevTest is a dev sandbox component — replace at-button elements with shadcn Button -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix TypeScript errors in route handlers</name>
  <files>src/app/api/collections/route.ts, src/app/api/database/test/route.ts</files>
  <action>
    **Fix 1 — collections/route.ts POST body sourceMetadata type:**
    At line 33, the request body type declares `sourceMetadata` as `{ repo, branch, path } | null`.
    Update it to use `ISourceMetadata | null` by importing `ISourceMetadata` from `@/types/collection.types` at the top of the file and changing the inline type.
    No runtime behavior changes — purely a type annotation fix.

    **Fix 2 — database/test/route.ts Connection type:**
    1. Update the import line to: `import mongoose, { Connection } from 'mongoose';`
    2. Change `let testConnection: typeof mongoose | null = null;` → `let testConnection: Connection | null = null;`
    3. The `.host`, `.port`, `.db`, `.close()` properties all exist on `Connection`, so errors at lines 34-36 and 62 will resolve automatically.
    4. Check if line 34 still has a null error after the type fix — if TypeScript still complains about `testConnection` being possibly null, use `testConnection!.host` (non-null assertion is acceptable here because the code is inside a try block after the await that assigns it) or add a null guard.

    Do NOT add @ts-ignore or `as any`. Verify with: `npx tsc --noEmit 2>&1 | grep -E "collections/route|database/test"` — should return zero lines.
  </action>
  <verify>
    <automated>cd /home/rikis/dev/tokens-manager && npx tsc --noEmit 2>&1 | grep -E "api/collections/route|api/database/test" | wc -l</automated>
  </verify>
  <done>Output is 0 — no TS errors in these two route files</done>
</task>

<task type="auto">
  <name>Task 2: Fix tokens page callback signature and AtuiDevTest</name>
  <files>src/app/collections/[id]/tokens/page.tsx, src/components/AtuiDevTest.tsx</files>
  <action>
    **Fix 3 — tokens/page.tsx handleTokensChange signature:**
    At line ~192, find `handleTokensChange`:
    ```typescript
    const handleTokensChange = useCallback((tokens: Record<string, unknown>) => {
    ```
    Check `TokenGeneratorFormNew`'s `onTokensChange` prop type (in TokenGeneratorFormNew.tsx interface). The prop expects `(tokens: Record<string, unknown> | null, namespace: string, collectionName: string) => void`.
    Update `handleTokensChange` to match this signature:
    ```typescript
    const handleTokensChange = useCallback((tokens: Record<string, unknown> | null, namespace: string, collectionName: string) => {
    ```
    Handle the `null` case: if tokens is null, treat as empty `{}` (use `tokens ?? {}`).
    Also use the additional `namespace` and `collectionName` params if there are state setters for them in scope, otherwise just accept and ignore them with `_namespace` and `_collectionName` if unused.

    **Fix 4 — AtuiDevTest.tsx ATUI stencil loader replacement:**
    Per user decision: "Replace the ATUI stencil loader in AtuiDevTest.tsx with a shadcn equivalent."
    Rewrite AtuiDevTest.tsx to be a shadcn-based sandbox component:
    - Remove the CSS import that bypasses the package exports field
    - Remove the `declare global namespace JSX IntrinsicElements` block for `at-button`
    - Remove the `useEffect` that dynamically imports the stencil loader
    - Replace `<at-button>` elements with shadcn `<Button>` from `@/components/ui/button`
    - Update the page description text to reflect it's now a shadcn component sandbox
    - Keep the file as a demo/sandbox page (it lives at /dev-test route)

    Do NOT add @ts-ignore or `as any`.
  </action>
  <verify>
    <automated>cd /home/rikis/dev/tokens-manager && npx tsc --noEmit 2>&1 | wc -l</automated>
  </verify>
  <done>npx tsc --noEmit outputs 0 lines (zero TypeScript errors)</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` exits with no errors and produces no output.
`grep -rn "@ts-ignore\|as any" src/app/api/ src/components/AtuiDevTest.tsx` — should return zero results (no suppressors added).
</verification>

<success_criteria>
- Zero TypeScript errors in the project
- No type suppressors introduced
- AtuiDevTest renders shadcn Buttons with no stencil loader dependency
</success_criteria>

<output>
After completion, create `.planning/phases/08-clean-code/08-02-SUMMARY.md` documenting:
- Each TS error fixed and what the fix was
- The AtuiDevTest replacement approach
- Final tsc output confirming zero errors
</output>
