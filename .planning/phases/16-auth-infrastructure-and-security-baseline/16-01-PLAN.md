---
phase: 16-auth-infrastructure-and-security-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/types/next-auth.d.ts
autonomous: true
requirements:
  - ARCH-01
  - AUTH-06

must_haves:
  truths:
    - "next@13.5.9 and eslint-config-next@13.5.9 are installed (CVE-2025-29927 patched)"
    - "next-auth@^4.24.13 and bcryptjs@^2.4.3 are installed with @types/bcryptjs"
    - "yarn build passes with zero TypeScript errors after package changes"
    - "session.user.id and session.user.role are typed without casting in all TypeScript files"
  artifacts:
    - path: "package.json"
      provides: "Updated dependency versions"
      contains: "13.5.9"
    - path: "src/types/next-auth.d.ts"
      provides: "TypeScript module augmentation for NextAuth Session and JWT"
      exports: ["Session augmentation with id and role", "JWT augmentation with id and role"]
  key_links:
    - from: "src/types/next-auth.d.ts"
      to: "next-auth Session interface"
      via: "declare module 'next-auth'"
      pattern: "declare module ['\"]next-auth['\"]"
    - from: "src/types/next-auth.d.ts"
      to: "next-auth/jwt JWT interface"
      via: "declare module 'next-auth/jwt'"
      pattern: "declare module ['\"]next-auth/jwt['\"]"
---

<objective>
Patch CVE-2025-29927 by upgrading Next.js to 13.5.9, install NextAuth v4 and bcryptjs packages, and add TypeScript module augmentation so `session.user.id` and `session.user.role` are strongly typed project-wide.

Purpose: Security patch must ship first — the CVE (CVSS 9.1) allows middleware auth bypass; no auth code should run on the vulnerable version. The TypeScript augmentation must exist before any file references `session.user.id` or `session.user.role`, which begins in Plan 03.
Output: Updated package.json, lockfile, and src/types/next-auth.d.ts.
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-auth-infrastructure-and-security-baseline/16-CONTEXT.md
@.planning/phases/16-auth-infrastructure-and-security-baseline/16-RESEARCH.md
@src/lib/db/models/TokenCollection.ts
@src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch CVE-2025-29927 and install auth packages</name>
  <files>package.json</files>
  <action>
    In package.json, make these exact changes to version strings (not ranges):
    - `"next": "13.5.9"` (was 13.5.6 — security patch for CVE-2025-29927)
    - `"eslint-config-next": "13.5.9"` in devDependencies (was 13.5.6 — must match next version exactly to avoid peer dep resolution errors per RESEARCH.md Pitfall 5)

    Then run:
    ```
    yarn add next-auth@^4.24.13 bcryptjs@^2.4.3
    yarn add -D @types/bcryptjs@^2.4.6
    ```

    Then run `yarn install` to update the lockfile.

    Do NOT use `bcrypt` (native addon) — use `bcryptjs` (pure JS). Per RESEARCH.md, native bcrypt fails to compile in Next.js / serverless environments.

    After installation, verify `yarn build` produces zero TypeScript errors. If `yarn build` fails, diagnose and fix before proceeding.
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && node -e "const pkg = require('./package.json'); console.log('next:', pkg.dependencies.next, 'next-auth:', pkg.dependencies['next-auth'], 'bcryptjs:', pkg.dependencies.bcryptjs); process.exit(pkg.dependencies.next === '13.5.9' ? 0 : 1)"</automated>
    <manual>Run `yarn build` and confirm zero TypeScript errors in terminal output</manual>
  </verify>
  <done>package.json shows next@13.5.9, eslint-config-next@13.5.9, next-auth@^4.24.13, bcryptjs@^2.4.3, @types/bcryptjs@^2.4.6; yarn build passes with zero TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Add TypeScript module augmentation for NextAuth Session and JWT</name>
  <files>src/types/next-auth.d.ts</files>
  <action>
    Create `src/types/next-auth.d.ts` with the following content exactly (the tsconfig.json `include: ["**/*.ts"]` pattern already covers this file — no tsconfig changes needed):

    ```typescript
    import NextAuth, { DefaultSession } from 'next-auth';
    import { JWT } from 'next-auth/jwt';

    declare module 'next-auth' {
      interface Session {
        user: {
          id: string;
          role: string;
        } & DefaultSession['user'];
      }
    }

    declare module 'next-auth/jwt' {
      interface JWT {
        id?: string;
        role?: string;
      }
    }
    ```

    This file goes in `src/types/` alongside the existing `*.types.ts` files. It does NOT export anything — `.d.ts` augmentation files are ambient declarations.

    After creating the file, run `yarn build` to confirm the augmentation compiles without errors. If TypeScript cannot find `next-auth` types (because packages aren't installed yet), ensure Task 1 completed first.
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && yarn build 2>&1 | tail -5</automated>
    <manual>Confirm no TypeScript error mentioning "Property 'id' does not exist on type" or "Property 'role' does not exist on type" in the build output</manual>
  </verify>
  <done>src/types/next-auth.d.ts exists; yarn build passes with zero TypeScript errors; session.user.id and session.user.role are typed without casting</done>
</task>

</tasks>

<verification>
- `node -e "require('./package.json').dependencies.next" && echo "next version: $(node -e "console.log(require('./package.json').dependencies.next)")"` outputs `13.5.9`
- `ls node_modules/next-auth/package.json` — package exists
- `ls node_modules/bcryptjs/package.json` — package exists
- `ls src/types/next-auth.d.ts` — file exists
- `yarn build` — exits 0, zero TypeScript errors
</verification>

<success_criteria>
- CVE-2025-29927 is patched: next@13.5.9 installed
- next-auth@^4.24.13 and bcryptjs@^2.4.3 are available in node_modules
- TypeScript module augmentation compiles; `session.user.id` and `session.user.role` are strongly typed
- yarn build passes with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-auth-infrastructure-and-security-baseline/16-01-SUMMARY.md`
</output>
