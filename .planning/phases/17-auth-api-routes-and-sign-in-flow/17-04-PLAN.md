---
phase: 17-auth-api-routes-and-sign-in-flow
plan: 04
type: execute
wave: 3
depends_on: [17-03]
files_modified: []
autonomous: false
requirements: [AUTH-01, AUTH-03, AUTH-04, AUTH-05]

must_haves:
  truths:
    - "User can sign in with correct email and password and land on the collections page"
    - "Wrong email produces 'No account found with that email' inline error"
    - "Wrong password produces 'Incorrect password' inline error"
    - "After sign-in, refreshing the browser preserves the session (user stays signed in)"
    - "Clicking Sign out from the UserMenu dropdown redirects to /auth/sign-in with no session"
    - "First-user setup flow: navigating to /auth/setup with no users shows the setup form"
    - "Completing setup auto signs in and redirects to the collections page"
    - "Navigating to /auth/setup after setup is complete redirects to /auth/sign-in"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete auth round-trip. Plans 01-03 built the backend (authorize errors, setup API), the providers (SessionProvider, PermissionsProvider), and the UI (sign-in page, setup page, UserMenu). This checkpoint confirms all four requirements (AUTH-01, AUTH-03, AUTH-04, AUTH-05) work end to end in a running browser.

Purpose: Automated build checks confirm TypeScript correctness but cannot verify: correct error messages appear inline, session persists across refresh, sign-out redirects correctly, or setup redirect logic works.
Output: Human confirmation that Phase 17 is complete.
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-auth-api-routes-and-sign-in-flow/17-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human verify: complete auth round-trip (5 scenarios)</name>
  <action>Start dev server with `yarn dev`. Walk through all 5 verification scenarios described in how-to-verify. Type "approved" when all pass.</action>
  <verify>User types "approved" in resume-signal confirming all 5 auth scenarios work end to end.</verify>
  <done>All 5 scenarios approved by user. Sign-in, session persistence, sign-out, setup flow, and UserMenu all work correctly in the browser.</done>
  <what-built>
Complete auth round-trip:
- `/auth/sign-in` — centered card, email/password form, inline errors, loading state (Plan 03 Task 1)
- `/auth/setup` — first-user bootstrap with display name + password, auto sign-in, redirect guard (Plan 03 Task 2)
- `UserMenu` in OrgHeader — initials avatar + display name + Sign out dropdown (Plan 03 Task 3)
- `AuthProviders` wrapping all client components — SessionProvider + PermissionsProvider (Plan 02)
- `authorize()` modified for specific error messages + `name` in session (Plan 01)
- `POST /api/auth/setup` + `GET /api/auth/setup` routes (Plan 01)
  </what-built>
  <how-to-verify>
Run the dev server first: `yarn dev` from the project root.

**Test 1 — Sign-in error messages (AUTH-01):**
1. Visit http://localhost:3000/auth/sign-in
2. Enter a non-existent email + any password → submit
3. Expected: error "No account found with that email" appears inline below the form
4. Enter the correct email (SUPER_ADMIN_EMAIL from .env.local) + wrong password → submit
5. Expected: error "Incorrect password" appears inline

**Test 2 — First-user setup (AUTH-05):**
(Only if no users exist in DB yet — clear users collection if needed)
1. Visit http://localhost:3000/auth/setup
2. Expected: form appears with display name + password fields
3. Fill in display name + password (8+ chars) + confirm password → submit
4. Expected: auto signed in, redirected to http://localhost:3000/
5. Visit http://localhost:3000/auth/setup again
6. Expected: immediately redirected to /auth/sign-in (setup already done)

**Test 3 — Successful sign-in (AUTH-01):**
1. Visit http://localhost:3000/auth/sign-in
2. Enter correct email (SUPER_ADMIN_EMAIL) + correct password → submit
3. Expected: redirected to http://localhost:3000/ (collections list)
4. Check OrgHeader top-right: initials circle + display name visible

**Test 4 — Session persistence (AUTH-03):**
1. While signed in, refresh the browser (Cmd+R / F5)
2. Expected: still signed in; UserMenu still shows name; collections page loads normally

**Test 5 — Sign out (AUTH-04):**
1. While signed in on any page, click the initials avatar in OrgHeader
2. Expected: dropdown appears with "Sign out" item
3. Click "Sign out"
4. Expected: redirected to http://localhost:3000/auth/sign-in
5. Visit http://localhost:3000/ — confirm not automatically signed back in
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 tests pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Human verifies:
- Sign-in error messages are specific (not generic "CredentialsSignin")
- Setup flow creates account, auto signs in, and becomes unreachable after completion
- Session persists across browser refresh
- Sign out redirects to /auth/sign-in
- UserMenu shows correct display name
</verification>

<success_criteria>
All 5 verification tests pass. Phase 17 is complete: users can sign in (AUTH-01), session persists (AUTH-03), users can sign out from any page (AUTH-04), and the first user gets Admin role via the setup flow (AUTH-05).
</success_criteria>

<output>
After completion, create `.planning/phases/17-auth-api-routes-and-sign-in-flow/17-04-SUMMARY.md`
</output>
