---
phase: 06-collection-ux-improvements
plan: "03"
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified: []
autonomous: false
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - UX-05

must_haves:
  truths:
    - "View tab and Generate tab both work end-to-end"
    - "Shared header controls affect both tabs"
    - "/generate redirects to /?tab=generate"
    - "Save As Collection saves and updates the selector"
    - "New Collection clears the form and switches to Generate"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Fully functional unified tabbed page"
    - path: "src/components/SharedCollectionHeader.tsx"
      provides: "Shared header rendered above both tabs"
    - path: "src/app/generate/page.tsx"
      provides: "Redirect to /?tab=generate"
  key_links:
    - from: "shared header Save As"
      to: "new collection in selector"
      via: "POST /api/collections + setCollections + handleSelectionChange"
      pattern: "setSaveAsDialogOpen"
---

<objective>
Human verification of the complete Phase 6 implementation.

Purpose: Confirms all locked decisions are working correctly in the browser before the phase is marked complete.
Output: Phase 6 verified — unified tabbed page working end-to-end.
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-collection-ux-improvements/06-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human verify: Phase 6 unified tabbed page end-to-end</name>
  <action>Pause for human verification of the complete Phase 6 implementation. No automated action required.</action>
  <what-built>
    Complete Phase 6 — unified tabbed page at / with shared collection header, View tab, Generate tab, and /generate redirect.

    Plans 06-01 and 06-02 implemented:
    - SharedCollectionHeader with selector, Save As Collection, New Collection, CollectionActions
    - page.tsx restructured with ?tab=view|generate URL control
    - View tab: read-only token table
    - Generate tab: full TokenGeneratorFormNew with all existing functionality
    - /generate redirects to /?tab=generate
  </what-built>
  <how-to-verify>
    Run the dev server: `cd /Users/user/Dev/atui-tokens-manager && npm run dev`

    Test the following flows:

    1. Tab navigation
       - Visit http://localhost:3000
       - Page loads on View tab (default)
       - Click "Generate Tokens" tab — URL changes to /?tab=generate, generator form loads
       - Click "View Tokens" tab — URL changes back to /, token table shows
       - Visit http://localhost:3000/generate directly — should redirect to /?tab=generate

    2. Shared header always visible
       - Collection selector is visible above tabs on both View and Generate tabs
       - "Save As Collection" button visible on both tabs
       - "New Collection" button visible on both tabs
       - CollectionActions (Delete/Rename/Duplicate) appear when a MongoDB collection is selected

    3. Collection selection affects both tabs
       - Select a MongoDB collection in the shared header
       - Switch between View and Generate tabs — same collection remains selected

    4. View tab (read-only)
       - Token table renders for selected collection
       - No inline editing buttons in the View tab

    5. Generate tab
       - TokenGeneratorFormNew form is visible
       - Load Collection, Save to Database, Preview JSON, GitHub Import buttons present inside the form
       - Load a collection via "Load Collection" inside the form — form populates
       - Switch to View tab and back — form state is preserved (data not lost)

    6. New Collection flow
       - Click "New Collection" in shared header
       - Generator form clears (blank token editor)
       - Active tab switches to Generate
       - Shared selector switches to "Local Files"

    7. Save As Collection flow
       - Select a MongoDB collection
       - Click "Save As Collection" in shared header
       - Dialog prompts for a name
       - After save: new collection appears in selector, toast confirms

    8. Build Tokens
       - On View tab with MongoDB collection selected: Build Tokens button enabled
       - On Generate tab with tokens loaded: Build Tokens button enabled
       - On View tab with Local Files: Build Tokens button disabled
       - Build Tokens modal opens and shows formatted output

    9. TypeScript build
       - Run: `cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit`
       - Output: zero errors
  </how-to-verify>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Human walks through flows 1-9 described in how-to-verify</manual>
  </verify>
  <done>All 9 verification flows pass; TypeScript compiles cleanly; human types "approved"</done>
  <resume-signal>Type "approved" if all flows work correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human confirms all 9 flows above pass.
</verification>

<success_criteria>
- All tab navigation works with correct URLs
- Shared header visible and functional on both tabs
- View tab read-only token display works
- Generate tab with full form works
- /generate redirects correctly
- New Collection and Save As flows work
- Build Tokens works from both tabs
- TypeScript builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06-collection-ux-improvements/06-03-SUMMARY.md`
</output>
