---
phase: 03-generator-form
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/collections/route.ts
  - src/app/api/collections/[id]/route.ts
  - .planning/ANGULAR_PARITY.md
autonomous: true
requirements: [GEN-01, GEN-02, GEN-03, GEN-04, MGMT-01]

must_haves:
  truths:
    - "POST /api/collections creates a new TokenCollection document and returns it"
    - "PUT /api/collections/[id] updates name/tokens/sourceMetadata and returns the updated document"
    - "POST returns 409 with { error, existingId } when a collection with the same name already exists"
    - "ANGULAR_PARITY.md documents POST and PUT contracts"
  artifacts:
    - path: "src/app/api/collections/route.ts"
      provides: "POST handler for creating a new collection"
      contains: "export async function POST"
    - path: "src/app/api/collections/[id]/route.ts"
      provides: "PUT handler for updating an existing collection"
      contains: "export async function PUT"
    - path: ".planning/ANGULAR_PARITY.md"
      provides: "POST and PUT route contracts documented"
      contains: "POST /api/collections"
  key_links:
    - from: "src/app/api/collections/route.ts"
      to: "src/lib/db/models/TokenCollection"
      via: "TokenCollection.create() for POST"
      pattern: "TokenCollection\\.create"
    - from: "src/app/api/collections/[id]/route.ts"
      to: "src/lib/db/models/TokenCollection"
      via: "TokenCollection.findByIdAndUpdate() for PUT"
      pattern: "findByIdAndUpdate"
---

<objective>
Create the two write-side API endpoints that the generator form needs: POST /api/collections (save new) and PUT /api/collections/[id] (overwrite existing). Also document both in ANGULAR_PARITY.md.

Purpose: The save dialog and overwrite flow in Plan 02 cannot function without these endpoints. All token-mutation logic lives server-side; the client only sends JSON and receives the saved document back.
Output: Two route handlers (POST added to existing route.ts, PUT added to existing [id]/route.ts), updated ANGULAR_PARITY.md.
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-view-integration/02-01-SUMMARY.md
@src/app/api/collections/route.ts
@src/app/api/collections/[id]/route.ts
@src/lib/db/models/TokenCollection.ts
@src/types/collection.types.ts
@.planning/ANGULAR_PARITY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add POST /api/collections handler</name>
  <files>src/app/api/collections/route.ts</files>
  <action>
Add a POST handler to the existing file (which currently only has GET). The handler must:

1. Call `await dbConnect()` first.
2. Parse request body: `{ name: string; tokens: Record<string, unknown>; sourceMetadata?: { repo: string|null; branch: string|null; path: string|null } | null }`.
3. Validate: if `name` is missing or empty string, return `NextResponse.json({ error: 'name is required' }, { status: 400 })`.
4. Duplicate-name check: `const existing = await TokenCollection.findOne({ name: body.name }).lean()`. If found, return `NextResponse.json({ error: \`A collection named "${body.name}" already exists\`, existingId: existing._id.toString() }, { status: 409 })`. The 409 + existingId is the hook the Save dialog uses to offer an overwrite flow.
5. Create: `const doc = await TokenCollection.create({ name: body.name, tokens: body.tokens, sourceMetadata: body.sourceMetadata ?? null, userId: null })`.
6. Return `NextResponse.json({ collection: { _id: doc._id.toString(), name: doc.name, tokens: doc.tokens, sourceMetadata: doc.sourceMetadata, userId: doc.userId, createdAt: (doc.createdAt as Date).toISOString(), updatedAt: (doc.updatedAt as Date).toISOString() } }, { status: 201 })`.
7. Wrap in try/catch; on error: `console.error('[POST /api/collections]', error)` and return `{ error: 'Failed to create collection' }` with status 500.

Keep the existing GET handler unchanged.
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit 2>&1 | grep "src/app/api/collections/route.ts" || echo "NO_ERRORS_IN_FILE"</automated>
    <manual>Optional: curl -X POST http://localhost:3000/api/collections -H 'Content-Type: application/json' -d '{"name":"test-plan-01","tokens":{}}' — expect 201 with collection object</manual>
  </verify>
  <done>POST /api/collections accepts { name, tokens, sourceMetadata? }, returns 201 + collection on success, 400 if name missing, 409 + existingId if name already taken, 500 on error. TypeScript reports no errors in this file.</done>
</task>

<task type="auto">
  <name>Task 2: Add PUT /api/collections/[id] handler + update ANGULAR_PARITY.md</name>
  <files>
    src/app/api/collections/[id]/route.ts
    .planning/ANGULAR_PARITY.md
  </files>
  <action>
**Part A — PUT handler:**

Add a PUT handler to the existing [id]/route.ts (which currently only has GET). The handler must:

1. Call `await dbConnect()` first.
2. Parse request body: `UpdateTokenCollectionInput` = `Partial<Pick<ITokenCollection, 'name' | 'tokens' | 'sourceMetadata'>>`.
3. If body is empty (all fields undefined), return `NextResponse.json({ error: 'Nothing to update' }, { status: 400 })`.
4. Update: `const doc = await TokenCollection.findByIdAndUpdate(params.id, { $set: body }, { new: true, runValidators: true }).lean()`.
5. If `doc` is null, return `NextResponse.json({ error: 'Collection not found' }, { status: 404 })`.
6. Return `NextResponse.json({ collection: { _id: doc._id.toString(), name: doc.name as string, tokens: doc.tokens, sourceMetadata: doc.sourceMetadata, userId: doc.userId, createdAt: (doc.createdAt as Date).toISOString(), updatedAt: (doc.updatedAt as Date).toISOString() } })`.
7. Wrap in try/catch; on error: `console.error('[PUT /api/collections/[id]]', error)` and return `{ error: 'Failed to update collection' }` with status 500.

Keep the existing GET handler unchanged.

**Part B — ANGULAR_PARITY.md:**

Add a new "Phase 3 — Generator Form" section after the existing Phase 1 section. Document:
- POST /api/collections: method, path, purpose, request body shape, response shape (201 success, 400 missing name, 409 duplicate with existingId, 500 error)
- PUT /api/collections/[id]: method, path, purpose, request body shape (UpdateTokenCollectionInput), response shape (200 success, 400 empty body, 404 not found, 500 error)
- The 409 + existingId pattern (client receives existingId and can call PUT to overwrite)
- Save dialog UI contract: name prompt modal, pre-fill with loaded collection name, duplicate-name confirmation before overwrite
- Load dialog UI contract: scrollable list of collection names, empty state message, unsaved-changes confirmation before replacing form state
- "Editing: [name]" indicator: displayed near Save/Load buttons when a loaded collection is active; cleared on Clear Form
- Dirty flag: set on any tokenGroups/globalNamespace change after last save or load; triggers unsaved-changes confirmation on Load
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit 2>&1 | grep "src/app/api/collections/\[id\]/route.ts" || echo "NO_ERRORS_IN_FILE"</automated>
    <manual>Optional: curl -X PUT http://localhost:3000/api/collections/{id} -H 'Content-Type: application/json' -d '{"name":"renamed"}' — expect 200 with updated collection</manual>
  </verify>
  <done>PUT /api/collections/[id] accepts UpdateTokenCollectionInput, returns 200 + collection on success, 400 if body empty, 404 if id unknown, 500 on error. ANGULAR_PARITY.md has Phase 3 section with POST and PUT contracts documented. TypeScript reports no errors in [id]/route.ts.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` reports no errors in src/app/api/collections/route.ts or src/app/api/collections/[id]/route.ts
- GET /api/collections still works (not broken by POST addition)
- GET /api/collections/[id] still works (not broken by PUT addition)
- ANGULAR_PARITY.md contains "Phase 3" section with POST and PUT documented
</verification>

<success_criteria>
1. POST /api/collections is callable and creates documents
2. PUT /api/collections/[id] is callable and updates documents
3. 409 response from POST includes `existingId` field the client can use for overwrite
4. No TypeScript compilation errors in either route file
5. ANGULAR_PARITY.md updated with Phase 3 API contracts
</success_criteria>

<output>
After completion, create `.planning/phases/03-generator-form/03-01-SUMMARY.md`
</output>
