---
phase: 07-fix-figma-integration
plan: 06
type: execute
wave: 3
depends_on: [07-03, 07-04, 07-05]
files_modified: []
autonomous: false
requirements: [FIGMA-01, FIGMA-02, FIGMA-03, FIGMA-04, FIGMA-05]

must_haves:
  truths:
    - "FigmaConfig button appears in app header; green dot shows after test-and-save"
    - "Export to Figma opens a dialog with collection picker (not prompt())"
    - "Import from Figma button in Generate tab completes the full picker-name-save flow"
    - "Source context bar appears for GitHub and Figma sourced collections, hidden for local/manual collections"
    - "TypeScript build has no errors"
  artifacts:
    - path: "src/components/FigmaConfig.tsx"
      provides: "Figma credentials dialog in app header"
    - path: "src/components/ExportToFigmaDialog.tsx"
      provides: "Export dialog with collection picker"
    - path: "src/components/ImportFromFigmaDialog.tsx"
      provides: "Two-step import dialog"
    - path: "src/components/SourceContextBar.tsx"
      provides: "Slim upstream source indicator bar"
  key_links:
    - from: "FigmaConfig button"
      to: "/api/figma/test"
      via: "Test Connection click"
      pattern: "test"
    - from: "Import from Figma button"
      to: "/api/figma/import"
      via: "ImportFromFigmaDialog confirm"
      pattern: "import"
---

<objective>
Human verification of the complete Phase 7 Figma integration. All five capabilities are tested end-to-end before marking the phase complete.

Purpose: Catch any visual, functional, or UX regressions that automated TypeScript checks cannot catch.

Output: Verified working Figma integration; any issues discovered and fixed before phase close.
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification</name>
  <files></files>
  <action>
Run a full TypeScript check and development server start to verify no build errors:

```bash
npx tsc --noEmit
```

If TypeScript errors exist, fix them before proceeding to the human verification checkpoint.

Also verify all new files exist:
- src/components/FigmaConfig.tsx
- src/components/ExportToFigmaDialog.tsx
- src/components/ImportFromFigmaDialog.tsx
- src/components/SourceContextBar.tsx
- src/app/api/figma/test/route.ts
- src/app/api/figma/collections/route.ts
- src/app/api/figma/import/route.ts
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1; ls src/components/FigmaConfig.tsx src/components/ExportToFigmaDialog.tsx src/components/ImportFromFigmaDialog.tsx src/components/SourceContextBar.tsx src/app/api/figma/test/route.ts src/app/api/figma/collections/route.ts src/app/api/figma/import/route.ts</automated>
  </verify>
  <done>TypeScript compiles clean; all 7 new files exist</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify complete Figma integration</name>
  <what-built>
Complete Phase 7 Figma integration:
1. FigmaConfig button in app header (alongside GitHubConfig)
2. Fixed Export to Figma dialog (proper dialog, collection picker, no more prompt())
3. Import from Figma button in Generate tab with two-step flow
4. Source context bar below tab switcher for GitHub/Figma sourced collections
5. Corrected X-Figma-Token auth header in export API
  </what-built>
  <how-to-verify>
Start the dev server: `npm run dev` and visit http://localhost:3000

**Test 1 — FigmaConfig button:**
1. App header should show two config buttons: "Configure GitHub" (or green dot if configured) AND "Configure Figma"
2. Click "Configure Figma" — dialog opens with PAT field and File URL field
3. Enter a Figma File URL — verify file key is auto-extracted and shown below the URL field
4. Click "Test Connection" — verify it attempts the test (loading state shows)
5. (If you have a real Figma PAT: verify green dot appears after successful test and save)

**Test 2 — Export to Figma dialog:**
1. Switch to Generate tab, load or create some tokens
2. Click "Export to Figma" button — verify a proper dialog opens (NOT a browser prompt())
3. If no Figma credentials configured: dialog shows "Configure Figma credentials first" message
4. If credentials configured: dialog shows the Figma file key and a collection picker dropdown
5. Close dialog without exporting

**Test 3 — Import from Figma:**
1. In Generate tab, verify "Import from Figma" button exists in the tab header area
2. Click it — dialog opens
3. If no credentials: shows "Configure credentials first" message
4. If credentials configured: shows loading → then list of Figma variable collections
5. (If you have a real Figma file: pick a collection, confirm name, verify it saves and auto-selects in shared header)

**Test 4 — Source context bar:**
1. Select "Local Files" from the collection selector — verify NO source bar appears
2. Select a MongoDB collection that was manually created — verify NO source bar appears
3. If you have a Figma-imported collection: select it — verify purple Figma bar appears with file key
4. (If you have a GitHub-imported collection: select it — verify gray GitHub bar appears with repo name)
5. Source bar should be between the tab switcher and the main content area

**Test 5 — Existing functionality not broken:**
1. GitHubConfig button still works (configure, connect)
2. Build Tokens button still works
3. Collection selector, Save As, Rename, Delete, Duplicate all still work
4. View and Generate tabs still switch correctly
  </how-to-verify>
  <files></files>
  <action>Human verifies the complete Figma integration end-to-end per the how-to-verify steps above.</action>
  <verify>
    <manual>All five test scenarios pass. No regressions in existing functionality.</manual>
  </verify>
  <done>User types "approved" confirming all Figma integration flows work correctly</done>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All five test scenarios pass human verification. No regressions in existing functionality.
</verification>

<success_criteria>
Phase 7 complete: Figma credentials dialog in header, working export dialog with collection picker, working import flow in Generate tab, source context bar for upstream-sourced collections, corrected X-Figma-Token auth header.
</success_criteria>

<output>
After completion, create `.planning/phases/07-fix-figma-integration/07-06-SUMMARY.md`
</output>
