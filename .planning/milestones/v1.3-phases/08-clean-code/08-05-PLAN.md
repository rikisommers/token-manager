---
phase: 08-clean-code
plan: 05
type: execute
wave: 4
depends_on:
  - "08-04"
files_modified: []
autonomous: false
requirements:
  - CLEAN-07

must_haves:
  truths:
    - "Collections page loads and displays collection cards correctly"
    - "Clicking a collection navigates to the token editor with correct content"
    - "Token groups tree is visible and selectable in the sidebar"
    - "Breadcrumb updates when selecting a tree node"
    - "TypeScript build is clean (zero errors)"
  artifacts:
    - path: "src/components/collections/"
      provides: "Populated feature domain directory"
    - path: "src/components/tokens/"
      provides: "Populated feature domain directory"
    - path: ".planning/phases/08-clean-code/REFACTOR-SUGGESTIONS.md"
      provides: "Out-of-scope suggestions for future phases"
  key_links:
    - from: "Browser UI"
      to: "Reorganized components"
      via: "Next.js routing + import resolution"
      pattern: "All pages render correctly after reorganization"
---

<objective>
Human verification that the refactored application works correctly end-to-end after all code quality improvements.

Purpose: Confirm that dead code removal, TS fixes, component reorganization, and SRP extraction haven't broken the UI or introduced regressions.
Output: Verified working application + sign-off that Phase 8 is complete.
</objective>

<execution_context>
@/home/rikis/.claude/get-shit-done/workflows/execute-plan.md
@/home/rikis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-clean-code/08-CONTEXT.md
@.planning/phases/08-clean-code/08-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final build verification</name>
  <files></files>
  <action>
    Run these verification commands in order:

    1. `cd /home/rikis/dev/tokens-manager && npx tsc --noEmit 2>&1` — should produce zero output (zero TS errors)
    2. `grep -rn "TokenGeneratorFormNew" src/` — should return zero results
    3. `grep -rn "collectionHeader" src/` — should return zero results (PascalCase fix confirmed)
    4. `ls src/components/*.tsx 2>&1` — should return "No such file or directory" (all components moved to subdirectories)
    5. `ls src/components/collections/index.ts src/components/tokens/index.ts src/components/layout/index.ts src/components/figma/index.ts src/components/github/index.ts src/components/dev/index.ts` — all six barrel files must exist
    6. `ls src/app/generate/ src/app/settings/ src/app/configuration/ 2>&1` — should return "No such file or directory" for all three
    7. `grep -rn "from 'react'\|from 'next/" src/utils/*.ts 2>/dev/null` — should return zero results (pure utils)
    8. `cat .planning/phases/08-clean-code/REFACTOR-SUGGESTIONS.md` — file exists with content

    Report the output of each command. If any command fails its expected result, fix the issue before proceeding to the human checkpoint.
  </action>
  <verify>
    <automated>cd /home/rikis/dev/tokens-manager && npx tsc --noEmit 2>&1 | wc -l</automated>
  </verify>
  <done>All 8 checks pass cleanly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human end-to-end verification</name>
  <files></files>
  <action>Present the verification steps below to the user and wait for their response.</action>
  <what-built>
    Complete Phase 8 clean code refactor:
    - Dead code removed (old TokenGeneratorForm, legacy routes generate/settings/configuration)
    - TokenGeneratorFormNew renamed to TokenGeneratorForm throughout
    - All 4 TypeScript errors fixed (collections route, database/test route, tokens page callback, AtuiDevTest)
    - AtuiDevTest migrated from broken ATUI stencil loader to shadcn components
    - 35 components reorganized into 6 feature domain folders (collections/, tokens/, layout/, figma/, github/, dev/) with barrel exports
    - collectionHeader.tsx renamed to CollectionHeader.tsx
    - SRP audit completed: non-rendering logic extracted to utils/services; REFACTOR-SUGGESTIONS.md created
    - get-repository.ts documented as the live factory entry point
  </what-built>
  <how-to-verify>
    Start the dev server if not already running: `npm run dev`

    1. Visit http://localhost:3000/collections — verify collection cards load correctly
    2. Click a collection → verify navigation to /collections/[id]/tokens works
    3. On the tokens page, verify the token groups tree appears in the left sidebar
    4. Click a tree node → verify breadcrumb updates and content scopes to that group
    5. Check the browser console — no "Cannot find module" or import errors should appear
    6. Visit http://localhost:3000/dev-test — verify AtuiDevTest page loads with shadcn buttons (no ATUI error)
    7. Confirm the legacy routes are gone: visiting /generate, /settings, /configuration should show Next.js 404
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint — awaiting human verification"</automated>
  </verify>
  <done>User types "approved" confirming all UI flows work correctly</done>
  <resume-signal>Type "approved" if everything works, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 8 automated checks pass.
Human confirms application works end-to-end in browser.
</verification>

<success_criteria>
- All Phase 8 work verified working in browser
- No regressions from the code quality improvements
- User signs off Phase 8 as complete
</success_criteria>

<output>
After completion, create `.planning/phases/08-clean-code/08-05-SUMMARY.md` documenting:
- Results of all 8 automated checks
- Human verification outcome
- Phase 8 complete status

Also update `.planning/STATE.md`:
- Current focus → Phase 8 complete
- Current position → Phase 8 of 8
- Last activity → today's date
</output>
