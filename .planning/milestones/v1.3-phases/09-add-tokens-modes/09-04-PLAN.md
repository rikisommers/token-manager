---
phase: 09-add-tokens-modes
plan: 04
type: execute
wave: 3
depends_on:
  - 09-02
  - 09-03
files_modified: []
autonomous: false
requirements:
  - MODE-01
  - MODE-02
  - MODE-03
  - MODE-04
  - MODE-05

must_haves:
  truths:
    - "Themes nav item appears in the collection sidebar and navigates to /collections/[id]/themes"
    - "User can create a new theme by name on the Themes page"
    - "Selecting a theme in the left panel shows all groups with their 3-state buttons"
    - "Clicking Disabled/Enabled/Source on a group row updates the state immediately"
    - "The theme selector on the Tokens page filters the group tree when a theme is selected"
    - "Selecting All groups restores all groups in the tree"
  artifacts:
    - path: "src/app/collections/[id]/themes/page.tsx"
      provides: "Themes page fully functional"
    - path: "src/components/collections/CollectionSidebar.tsx"
      provides: "Themes nav item present"
    - path: "src/app/collections/[id]/tokens/page.tsx"
      provides: "Theme selector integrated"
  key_links:
    - from: "CollectionSidebar Themes link"
      to: "src/app/collections/[id]/themes/page.tsx"
      via: "Next.js Link component"
      pattern: "themes"
---

<objective>
Human end-to-end verification of Phase 9 — confirm the full Themes feature works correctly in the browser.

Purpose: Validates that all data layer, UI, and navigation pieces work together as a cohesive feature.
Output: User approval that Phase 9 is complete.
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-add-tokens-modes/09-CONTEXT.md
@.planning/phases/09-add-tokens-modes/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build and type checks</name>
  <files></files>
  <action>
Run the following checks to confirm no regressions before human verification:

1. TypeScript check: `npx tsc --noEmit`
2. Next.js build: `npm run build`

Fix any TypeScript errors or build failures found. Report results.
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit 2>&1 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Zero TypeScript errors; `npm run build` exits 0</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human end-to-end verification of Themes feature</name>
  <files></files>
  <action>Human verifies the complete Themes feature in the browser following the checklist in how-to-verify.</action>
  <verify>
    <manual>Follow the 14-step verification checklist below</manual>
  </verify>
  <done>User types "approved" to signal all verification steps pass</done>
  <what-built>
    Full Phase 9 Themes feature:
    - Theme data model stored per-collection in MongoDB
    - /api/collections/[id]/themes CRUD routes
    - Themes page at /collections/[id]/themes with left panel (theme list) and right panel (group matrix with Disabled/Enabled/Source buttons)
    - Themes nav item in the collection sidebar
    - Theme selector dropdown on the Tokens page that filters the group tree
  </what-built>
  <how-to-verify>
    1. Open a collection that has token groups. Click "Themes" in the sidebar.
    2. Verify: Themes page loads with "No themes yet" empty state in the left panel.
    3. Click + and type a name (e.g. "Dark") then press Enter.
    4. Verify: Theme "Dark" appears in the left panel and is selected. All groups are shown in the right panel with "Enabled" as the active state.
    5. Click "Disabled" on one group row.
    6. Verify: That group row now shows "Disabled" highlighted. Navigate away and back to confirm persistence.
    7. Click + again, create a second theme (e.g. "Light").
    8. Verify: "Light" appears in the list. All groups default to "Disabled" (not "Enabled", since this is not the first theme).
    9. Navigate to the Tokens page for the same collection.
    10. Verify: A "Theme:" dropdown appears in the top bar with "All groups" as default.
    11. Select the "Dark" theme.
    12. Verify: The group you set to Disabled in step 5 disappears from the group tree. Other groups remain.
    13. Switch back to "All groups".
    14. Verify: All groups reappear in the tree — no permanent changes to token data.
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All automated checks pass (TypeScript + build).
Human verification approved per checklist above.
</verification>

<success_criteria>
- npm run build exits 0
- Zero TypeScript errors
- Human approves all 14 verification steps
</success_criteria>

<output>
After completion, create `.planning/phases/09-add-tokens-modes/09-04-SUMMARY.md`
</output>
