---
phase: 05-tree-data-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/tree.utils.ts
  - src/utils/index.ts
  - src/components/TokenGeneratorFormNew.tsx
autonomous: true
requirements:
  - TREE-02

must_haves:
  truths:
    - "parseGroupPath('brands/brand2/color.json') returns ['Brands', 'Brand2', 'Color']"
    - "parseGroupPath('globals/border-color.json') returns ['Globals', 'Border Color']"
    - "parseGroupPath('globals/border-radius.json') returns ['Globals', 'Border Radius']"
    - "onGroupsChange emits TokenGroup[] (with children) not { id, name }[] flat list"
  artifacts:
    - path: "src/utils/tree.utils.ts"
      provides: "parseGroupPath utility + buildDisplayLabel helper"
      exports: ["parseGroupPath", "buildDisplayLabel"]
    - path: "src/components/TokenGeneratorFormNew.tsx"
      provides: "onGroupsChange emitting full TokenGroup[] tree"
      contains: "onGroupsChange(tokenGroups)"
  key_links:
    - from: "src/components/TokenGeneratorFormNew.tsx"
      to: "src/utils/tree.utils.ts"
      via: "import parseGroupPath"
      pattern: "parseGroupPath"
    - from: "src/components/TokenGeneratorFormNew.tsx"
      to: "onGroupsChange prop"
      via: "calls onGroupsChange(tokenGroups) instead of flat summary"
      pattern: "onGroupsChange\\(tokenGroups\\)"
---

<objective>
Add the path-parsing utility and upgrade the onGroupsChange callback signature to emit the full TokenGroup[] tree.

Purpose: All downstream consumers (tokens page sidebar, Phase 6 selection) need the full tree structure — not just flat id+name pairs. The path parser is the foundation for correct display labels in the tree.
Output: src/utils/tree.utils.ts with parseGroupPath + buildDisplayLabel; TokenGeneratorFormNew emitting TokenGroup[] via onGroupsChange.
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tree-data-model/05-CONTEXT.md
@src/types/token.types.ts
@src/utils/token.utils.ts
@src/utils/index.ts
@src/components/TokenGeneratorFormNew.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tree.utils.ts with parseGroupPath and buildDisplayLabel</name>
  <files>src/utils/tree.utils.ts</files>
  <action>
Create `src/utils/tree.utils.ts` with two exported functions:

**`buildDisplayLabel(segment: string): string`**
- Replaces hyphens and underscores with spaces: `border-color` → `Border Color`, `border_radius` → `Border Radius`
- Capitalizes the first letter of each space-separated word
- Does NOT split on digit boundaries — `brand1`, `brand2` become `Brand1`, `Brand2`
- Numeric-only segments returned as-is (e.g. `"400"` → `"400"`)
- Implementation: `segment.replace(/[-_]/g, ' ').replace(/\b\w/g, c => c.toUpperCase())`

**`parseGroupPath(groupName: string): string[]`**
- Splits the group name by `/`
- Strips the `.json` extension from the LAST segment only (no-op if not present)
- Applies `buildDisplayLabel` to every segment
- Returns the array of display labels
- Examples (from locked decisions):
  - `'brands/brand1/color.json'` → `['Brands', 'Brand1', 'Color']`
  - `'brands/brand2/color.json'` → `['Brands', 'Brand2', 'Color']`
  - `'globals/border-color.json'` → `['Globals', 'Border Color']`
  - `'globals/border-radius.json'` → `['Globals', 'Border Radius']`
  - `'globals/breakpoint.json'` → `['Globals', 'Breakpoint']`

File should be pure TypeScript with no React/Next.js imports.
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "tree.utils" || echo "No tree.utils errors"</automated>
    <manual>Verify parseGroupPath cases manually: node -e "const {parseGroupPath}=require('./src/utils/tree.utils'); console.log(parseGroupPath('brands/brand2/color.json'))" (expect ['Brands','Brand2','Color'])</manual>
  </verify>
  <done>src/utils/tree.utils.ts exists, exports parseGroupPath and buildDisplayLabel, TypeScript compiles without errors for this file</done>
</task>

<task type="auto">
  <name>Task 2: Export tree.utils from index + upgrade onGroupsChange to emit TokenGroup[]</name>
  <files>src/utils/index.ts, src/components/TokenGeneratorFormNew.tsx</files>
  <action>
**Step A — Export from barrel:**
Add `export * from './tree.utils';` to `src/utils/index.ts`.

**Step B — Upgrade onGroupsChange in TokenGeneratorFormNew:**

1. Change the prop type signature (line ~58):
   ```typescript
   // Before:
   onGroupsChange?: (groups: { id: string; name: string }[]) => void;
   // After:
   onGroupsChange?: (groups: TokenGroup[]) => void;
   ```

2. Change onGroupAdded prop type too (line ~61) — it stays as `{ id: string; name: string }` (single group, not tree), no change needed there.

3. Find the `useEffect` that fires `onGroupsChange` (around line 312-318). It currently builds a flat summary:
   ```typescript
   const summary = tokenGroups.map(g => ({ id: g.id, name: g.name }));
   onGroupsChange(summary);
   ```
   Replace this with:
   ```typescript
   onGroupsChange(tokenGroups);
   ```
   Remove the `summary` variable entirely.

**Important:** `tokenGroups` is already typed as `TokenGroup[]` with `children?: TokenGroup[]` — no conversion needed. The existing `TokenGroup` interface in `src/types/token.types.ts` already has the `children` field.

**TypeScript check:** After this change, the tokens page `handleGroupsChange` callback will need its parameter type updated too. Find `handleGroupsChange` in `src/app/collections/[id]/tokens/page.tsx`:
- Change its parameter type from `(groups: { id: string; name: string }[])` to `(groups: TokenGroup[])` (import `TokenGroup` from `@/types`)
- Change `masterGroups` state type from `{ id: string; name: string }[]` to `TokenGroup[]`
- Update `setMasterGroups(groups)` — stays the same
- Update the `selectedGroupId` fallback: `groups[0]?.id ?? ''` — stays the same (TokenGroup has `id`)
- Update the group list render in the page — it currently maps `group.name` for display; for now keep rendering `group.name` directly (the tree component in Plan 02 will replace this entirely)

Run `npx tsc --noEmit` to confirm zero new TypeScript errors on the modified files.
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit 2>&1 | grep -E "TokenGeneratorFormNew|tokens/page|tree.utils|onGroupsChange" || echo "No errors in modified files"</automated>
  </verify>
  <done>onGroupsChange prop type is TokenGroup[], TokenGeneratorFormNew emits full tokenGroups tree, tokens page compiles without errors, barrel export updated</done>
</task>

</tasks>

<verification>
- `src/utils/tree.utils.ts` exists and exports `parseGroupPath`, `buildDisplayLabel`
- `parseGroupPath('globals/border-color.json')` returns `['Globals', 'Border Color']`
- `parseGroupPath('brands/brand2/color.json')` returns `['Brands', 'Brand2', 'Color']`
- `onGroupsChange` prop in `TokenGeneratorFormNew` accepts `TokenGroup[]`
- `masterGroups` state in tokens page is `TokenGroup[]`
- `npx tsc --noEmit` shows no new errors introduced by this plan
</verification>

<success_criteria>
- Path parser utility created and barrel-exported
- onGroupsChange emits the full TokenGroup[] tree (with children preserved)
- TypeScript compiles cleanly across all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/05-tree-data-model/05-01-SUMMARY.md`
</output>
