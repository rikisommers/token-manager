---
phase: 04-collection-management
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/collections/layout.tsx
  - src/components/LayoutShell.tsx
autonomous: true
requirements:
  - COL-02
  - COL-03

must_haves:
  truths:
    - "Navigating to / redirects to /collections immediately"
    - "The /collections route renders with NO sidebar and full-width layout"
    - "The root layout sidebar and header are NOT shown on /collections"
    - "Existing routes (/, /configuration, /settings, /generate) still render with the sidebar shell"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Redirect from / to /collections"
      contains: "redirect"
    - path: "src/app/collections/layout.tsx"
      provides: "Full-width layout for collections grid (no sidebar, no AppHeader)"
      contains: "children"
    - path: "src/components/LayoutShell.tsx"
      provides: "Client component that conditionally shows sidebar/header based on pathname"
      contains: "usePathname"
  key_links:
    - from: "src/app/layout.tsx"
      to: "AppSidebar + AppHeader"
      via: "conditional render — hide on /collections but show on collection-scoped pages"
      pattern: "usePathname|pathname"
    - from: "src/app/page.tsx"
      to: "/collections"
      via: "Next.js redirect()"
      pattern: "redirect"
---

<objective>
Restructure the root layout to conditionally show/hide the sidebar and header, redirect the home page to the collections grid, and create a collections-level layout file that provides a clean full-width shell for the grid.

Purpose: Phase 4 changes the app entry point from / (tokens page) to /collections (grid). The sidebar must not appear on the grid page per user decision.
Output: Updated root layout with conditional sidebar, redirect page, collections layout file.
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/layout.tsx
@src/app/page.tsx
@src/components/AppSidebar.tsx
@src/components/AppHeader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redirect home page and create collections route layout</name>
  <files>
    src/app/page.tsx
    src/app/collections/layout.tsx
  </files>
  <action>
In `src/app/page.tsx`:
- Replace the entire file content with a server component that redirects to /collections using Next.js `redirect()` from 'next/navigation'
- This must be a server component (no 'use client') to use redirect()
- Content: `import { redirect } from 'next/navigation'; export default function Home() { redirect('/collections'); }`

Create `src/app/collections/layout.tsx`:
- Server component (no 'use client')
- Renders children directly in a `div` with full-width styling: `className="min-h-screen bg-gray-50"`
- This layout wraps ONLY /collections (the grid page) — NOT /collections/[id] routes (those get their own layout in plan 04-04)
- Do NOT import AppSidebar or AppHeader here — the grid is deliberately sidebar-free
- Export default function `CollectionsLayout({ children }: { children: React.ReactNode })`
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit 2>&1 | grep -v "token-manager-angular\|token-manager-stencil\|token.service.ts" | head -20</automated>
    <manual>With dev server running: navigate to http://localhost:3000 — should redirect to /collections. The /collections page (even if empty) should not show the sidebar from the root layout.</manual>
  </verify>
  <done>/ redirects to /collections. src/app/collections/layout.tsx exists and renders children without sidebar. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Make root layout sidebar + header conditional (hide on /collections grid)</name>
  <files>
    src/app/layout.tsx
    src/components/LayoutShell.tsx
  </files>
  <action>
Update `src/app/layout.tsx` to conditionally hide the sidebar and header on the /collections grid page.

The root layout is currently a server component. To read the pathname, use a client wrapper component.

Strategy: Create an inline client component `LayoutShell` inside layout.tsx (or extract to src/components/LayoutShell.tsx) that:
1. Uses `usePathname()` from 'next/navigation'
2. Defines `const isCollectionsGrid = pathname === '/collections'`
3. If `isCollectionsGrid`: renders children directly (no AppHeader, no AppSidebar — they are already suppressed by the /collections/layout.tsx providing its own shell, but this ensures the root layout div wrappers don't add sidebar columns either)
4. If NOT isCollectionsGrid: renders the existing sidebar shell (AppHeader + flex div with AppSidebar + main)

Implementation detail: Extract the conditional layout into `src/components/LayoutShell.tsx` as a 'use client' component that accepts `children: React.ReactNode`. Move the `<CollectionProvider>` wrapping into LayoutShell so it wraps both paths. The root layout.tsx becomes a simple server component that renders `<LayoutShell>{children}</LayoutShell>` inside `<html><body>`.

Keep `CollectionProvider` wrapping ALL routes (collections grid still needs it for the New Collection card action).

Tailwind classes to preserve on the non-grid path:
- Outer div: `flex flex-col h-screen overflow-hidden bg-gray-50`
- Sidebar wrapper: `w-[200px] flex-shrink-0`
- Main: `flex-1 overflow-y-auto`
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit 2>&1 | grep -v "token-manager-angular\|token-manager-stencil\|token.service.ts" | head -20</automated>
    <manual>With dev server running: (1) visit /collections — no sidebar visible, full-width. (2) visit /configuration — sidebar visible. (3) visit /settings — sidebar visible. (4) visit /generate — sidebar visible.</manual>
  </verify>
  <done>LayoutShell component created. Root layout.tsx renders LayoutShell. /collections shows no sidebar. All other routes show sidebar + header. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` passes with no new errors.
/ redirects to /collections.
/collections renders without sidebar or header columns.
/configuration, /settings, /generate still show the sidebar shell.
</verification>

<success_criteria>
- Home page (/) redirects to /collections
- /collections renders with full-width layout (no sidebar)
- Existing sidebar-bearing routes unaffected
- LayoutShell client component correctly reads pathname and branches rendering
- Zero new TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-collection-management-grid-of-collections-collection-scoped-pages-with-sidebar-tokens-config-settings-per-collection-config-persisted-to-db/04-02-SUMMARY.md`
</output>
