---
phase: 04-collection-management
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/collection.types.ts
  - src/lib/db/models/TokenCollection.ts
  - src/app/api/collections/route.ts
  - src/app/api/collections/[id]/route.ts
  - src/app/api/collections/[id]/duplicate/route.ts
autonomous: true
requirements:
  - COL-01
  - COL-04
  - COL-05

must_haves:
  truths:
    - "GET /api/collections returns name, tokenCount, updatedAt, description, tags, and integration badge data (figmaConfigured, githubConfigured)"
    - "PUT /api/collections/[id] accepts and persists figmaToken, figmaFileId, githubRepo, githubBranch fields"
    - "POST /api/collections/[id]/duplicate creates a full copy including name (suffixed), tokens, and config fields"
    - "Existing collections without config fields return null for those fields without errors"
  artifacts:
    - path: "src/types/collection.types.ts"
      provides: "ITokenCollection with description, tags, figmaToken, figmaFileId, githubRepo, githubBranch fields"
      contains: "figmaToken"
    - path: "src/lib/db/models/TokenCollection.ts"
      provides: "Mongoose schema with new config fields"
      contains: "figmaToken"
    - path: "src/app/api/collections/[id]/duplicate/route.ts"
      provides: "POST endpoint for duplicating a collection"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/collections/route.ts"
      to: "TokenCollection.find"
      via: "updated projection to include new fields + tokenCount derived from tokens object"
      pattern: "TokenCollection\\.find"
    - from: "src/app/api/collections/[id]/duplicate/route.ts"
      to: "TokenCollection.findById + TokenCollection.create"
      via: "copy all fields including new config fields"
      pattern: "TokenCollection\\.create"
---

<objective>
Extend the MongoDB collection schema and API layer to support collection metadata (description, tags) and per-collection Figma/GitHub config fields. Update GET /api/collections to return richer card data. Add a duplicate endpoint.

Purpose: The collections grid and per-collection settings page both need richer API data. This is the data foundation everything else builds on.
Output: Extended TypeScript types, updated Mongoose schema, updated collection routes, new duplicate route.
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types/collection.types.ts
@src/lib/db/models/TokenCollection.ts
@src/app/api/collections/route.ts
@src/app/api/collections/[id]/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend collection types and Mongoose schema with metadata + config fields</name>
  <files>
    src/types/collection.types.ts
    src/lib/db/models/TokenCollection.ts
  </files>
  <action>
In `src/types/collection.types.ts`:
- Add optional fields to `ITokenCollection`: `description: string | null`, `tags: string[]`, `figmaToken: string | null`, `figmaFileId: string | null`, `githubRepo: string | null`, `githubBranch: string | null`
- Update `UpdateTokenCollectionInput` Pick to include the six new fields: `description`, `tags`, `figmaToken`, `figmaFileId`, `githubRepo`, `githubBranch`
- Add a `CollectionCardData` interface (for GET /api/collections list items): `{ _id: string; name: string; description: string | null; tags: string[]; tokenCount: number; updatedAt: string; figmaConfigured: boolean; githubConfigured: boolean }`

In `src/lib/db/models/TokenCollection.ts`:
- Add six new fields to the Mongoose schema (all with `default: null` or `default: []` for tags):
  - `description: { type: String, default: null }`
  - `tags: { type: [String], default: [] }`
  - `figmaToken: { type: String, default: null }`
  - `figmaFileId: { type: String, default: null }`
  - `githubRepo: { type: String, default: null }`
  - `githubBranch: { type: String, default: null }`
- These fields are optional on `TokenCollectionDoc` — use `Partial<Pick<...>>` or add them directly to the Omit type. Safest: add them directly to the schema type as optional properties.

Do NOT remove any existing fields. Do NOT change the timestamps or userId fields.
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit 2>&1 | grep -v "token-manager-angular\|token-manager-stencil\|token.service.ts" | head -20</automated>
    <manual>Confirm no new TypeScript errors introduced beyond the pre-existing ones in token.service.ts and angular/stencil subprojects</manual>
  </verify>
  <done>ITokenCollection has all six new fields. Mongoose schema has all six new fields with correct defaults. UpdateTokenCollectionInput includes the new fields. CollectionCardData interface exists. TypeScript compiles with no new errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update collection API routes — richer GET list, extended PUT, new duplicate endpoint</name>
  <files>
    src/app/api/collections/route.ts
    src/app/api/collections/[id]/route.ts
    src/app/api/collections/[id]/duplicate/route.ts
  </files>
  <action>
In `src/app/api/collections/route.ts` — update the GET handler:
- Change the projection in `TokenCollection.find()` to include all fields needed for the card: `{ name: 1, description: 1, tags: 1, tokens: 1, figmaToken: 1, figmaFileId: 1, githubRepo: 1, githubBranch: 1, updatedAt: 1 }`
- Compute `tokenCount` as `Object.keys(doc.tokens ?? {}).length`
- Compute `figmaConfigured` as `!!(doc.figmaToken && doc.figmaFileId)`
- Compute `githubConfigured` as `!!(doc.githubRepo)`
- Sort by `updatedAt` descending: add `.sort({ updatedAt: -1 })` to the query
- Return `CollectionCardData[]` shape (matches the new interface)

In `src/app/api/collections/[id]/route.ts` — the PUT handler already accepts `UpdateTokenCollectionInput`. The schema change means the new fields are valid in `$set` automatically. No structural change needed except updating the GET response to also return the six new config fields alongside existing fields (add them to the response object so the settings page can pre-populate).

Create `src/app/api/collections/[id]/duplicate/route.ts`:
- Export `async function POST(_request: Request, { params }: { params: { id: string } })`
- Fetch the source collection with `TokenCollection.findById(params.id).lean()`
- Return 404 if not found
- Create a new collection via `TokenCollection.create({...})` copying: tokens, description, tags, sourceMetadata, figmaToken, figmaFileId, githubRepo, githubBranch, userId
- Set name to `${source.name} (copy)` — if that name already exists, append a timestamp suffix: `${source.name} (copy ${Date.now()})`
- Try/catch with 409 handling for duplicate name (use the same duplicate-name check as POST /api/collections: findOne by name, if exists use timestamp suffix)
- Return 201 with the new collection's _id and name
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && npx tsc --noEmit 2>&1 | grep -v "token-manager-angular\|token-manager-stencil\|token.service.ts" | head -20</automated>
    <manual>Run dev server (yarn dev) and GET http://localhost:3000/api/collections — confirm response includes updatedAt, tokenCount, figmaConfigured, githubConfigured fields. POST to /api/collections/[id]/duplicate — confirm 201 with new _id.</manual>
  </verify>
  <done>GET /api/collections returns CollectionCardData[] sorted by updatedAt desc. GET /api/collections/[id] returns all six config fields. POST /api/collections/[id]/duplicate returns 201 with copied collection. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` from project root — no new TypeScript errors (pre-existing errors in token.service.ts and angular/stencil subprojects are acceptable).
GET /api/collections returns array with tokenCount, figmaConfigured, githubConfigured, updatedAt per item.
POST /api/collections/{id}/duplicate creates a new collection with all fields copied.
</verification>

<success_criteria>
- ITokenCollection has description, tags, figmaToken, figmaFileId, githubRepo, githubBranch
- Mongoose schema persists all six new fields
- GET /api/collections returns richer card data sorted by updatedAt desc
- Duplicate endpoint creates full copy with "(copy)" name suffix
- Zero new TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-collection-management-grid-of-collections-collection-scoped-pages-with-sidebar-tokens-config-settings-per-collection-config-persisted-to-db/04-01-SUMMARY.md`
</output>
