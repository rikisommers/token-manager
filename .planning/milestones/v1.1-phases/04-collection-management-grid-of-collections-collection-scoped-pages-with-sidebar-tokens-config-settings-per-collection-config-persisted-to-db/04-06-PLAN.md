---
phase: 04-collection-management
plan: "06"
type: execute
wave: 4
depends_on:
  - "04-03"
  - "04-04"
  - "04-05"
files_modified: []
autonomous: false
requirements:
  - COL-01
  - COL-02
  - COL-03
  - COL-04
  - COL-05
  - COL-06
  - COL-07
  - COL-08

must_haves:
  truths:
    - "/ redirects to /collections"
    - "Collections grid shows all collections as cards with correct metadata"
    - "Collection card kebab actions all work (rename, delete, duplicate)"
    - "Navigating into a collection shows CollectionSidebar with correct collection name"
    - "Config fields in /collections/[id]/settings auto-save and persist across page reload"
    - "Build looks clean (no TypeScript errors that block dev server)"
  artifacts:
    - path: "src/app/collections/page.tsx"
      provides: "Working collections grid"
      contains: "CollectionCard"
    - path: "src/app/collections/[id]/settings/page.tsx"
      provides: "Working per-collection settings with auto-save"
      contains: "auto-save"
  key_links:
    - from: "src/app/collections/page.tsx"
      to: "/collections/[id]/tokens"
      via: "card click navigation"
      pattern: "router\\.push"
---

<objective>
Human visual and functional verification of the complete Phase 4 collection management flow.

Purpose: Confirm the full flow works end-to-end before marking Phase 4 complete.
Output: Phase 4 signed off or issues filed for gap closure.
</objective>

<execution_context>
@/Users/user/.claude/get-shit-done/workflows/execute-plan.md
@/Users/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final build verification</name>
  <files></files>
  <action>
Run the TypeScript compiler and Next.js build to confirm there are no blocking errors:
1. Run `npx tsc --noEmit` — confirm no NEW errors beyond the pre-existing token.service.ts and angular/stencil errors
2. Run `yarn build` (Next.js production build) — confirm it completes successfully
3. If build fails: fix any import errors, missing modules, or type mismatches introduced in Phase 4 plans
  </action>
  <verify>
    <automated>cd /Users/user/Dev/atui-tokens-manager && yarn build 2>&1 | tail -20</automated>
  </verify>
  <done>yarn build completes with no errors. Any TypeScript errors are pre-existing (not introduced in Phase 4).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete collection management flow</name>
  <files></files>
  <action>
Start the dev server with `yarn dev` and walk through the full Phase 4 verification checklist below. Fix any issues found before returning "approved".
  </action>
  <verify>
    <manual>
Start dev server: `yarn dev`

1. Visit http://localhost:3000 — confirm redirect to /collections
2. Verify collections grid: cards visible with metadata (name, token count, last modified)
3. Hover a card — confirm kebab menu appears
4. Rename: click Rename in kebab, edit the name inline, press Enter — confirm name updates on card
5. Duplicate: click Duplicate — confirm new card appears with "(copy)" suffix
6. Delete: click Delete — confirm dialog appears with "Delete [name]? This will remove all tokens and config." — click Delete — confirm card removed
7. Click "+ New Collection" card — confirm new collection created and browser navigates to /collections/[id]/tokens
8. Verify CollectionSidebar: app name link at top, "Collections" back link below it, collection name label, Tokens/Config/Settings nav items visible
9. Click the app name at the top of the sidebar — confirm navigates back to /collections
10. Click "Collections" back link in sidebar — confirm navigates back to /collections
11. Click "Config" in sidebar — confirm /collections/[id]/config loads with BuildTokensPanel
12. Click "Settings" in sidebar — confirm /collections/[id]/settings loads with Figma + GitHub fields
13. Enter a value in "Figma File ID" — wait 2 seconds — confirm "Saved" indicator appears
14. Reload the settings page — confirm the saved value persists
15. Click "Clear" on Figma section — confirm fields empty + saved
    </manual>
  </verify>
  <done>All 15 verification steps pass. User types "approved".</done>
  <resume-signal>Type "approved" if all flows work, or describe specific issues found</resume-signal>
</task>

</tasks>

<verification>
All 15 verification steps above pass.
</verification>

<success_criteria>
- Complete collection management flow works end-to-end
- yarn build passes
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/04-collection-management-grid-of-collections-collection-scoped-pages-with-sidebar-tokens-config-settings-per-collection-config-persisted-to-db/04-06-SUMMARY.md`
</output>
